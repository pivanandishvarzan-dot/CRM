function erf(x:number){const s=x<0?-1:1,a=Math.abs(x),t=1/(1+.3275911*a),y=1-(((((1.061405429*t-1.453152027)*t)+1.421413741)*t-.284496736)*t+.254829592)*t*Math.exp(-a*a);return s*y}function phi(z:number){return .5*(1+erf(z/Math.sqrt(2)))}export function compareProportions(a:{evaluated:number;successes:number},b:{evaluated:number;successes:number}){const n1=Number(a.evaluated||0),n2=Number(b.evaluated||0),x1=Number(a.successes||0),x2=Number(b.successes||0);if(n1<2||n2<2)return{ready:false,confidence:0,lift:0,pValue:1,z:0,verdict:'INSUFFICIENT_SAMPLE'};const p1=x1/n1,p2=x2/n2,p=(x1+x2)/(n1+n2),se=Math.sqrt(p*(1-p)*(1/n1+1/n2));if(!se)return{ready:false,confidence:0,lift:0,pValue:1,z:0,verdict:'NO_VARIANCE'};const z=(p1-p2)/se,pValue=2*(1-phi(Math.abs(z))),confidence=Math.max(0,Math.min(99.9,(1-pValue)*100)),lift=p2?((p1-p2)/p2)*100:p1?100:0,ready=n1>=30&&n2>=30,verdict=!ready?'INSUFFICIENT_SAMPLE':pValue<=.05?(p1>p2?'SMART_WIN':'CONTROL_WIN'):'INCONCLUSIVE';return{ready,confidence:Math.round(confidence*10)/10,lift:Math.round(lift*10)/10,pValue:Math.round(pValue*10000)/10000,z:Math.round(z*100)/100,verdict}}
