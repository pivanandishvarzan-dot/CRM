export type TrustLevel='HIGH_TRUST'|'MODERATE_TRUST'|'LOW_TRUST'|'REVIEW_REQUIRED';export type AiRisk='LOW'|'MEDIUM'|'HIGH'|'CRITICAL';export type AiMode='AUTO'|'SUGGEST'|'MANAGER_APPROVAL'|'BLOCKED';const policies:Record<string,{risk:AiRisk;neverAuto?:boolean}>={LEAD_TAGGING:{risk:'LOW'},LEAD_PRIORITY_HINT:{risk:'LOW'},FOLLOW_UP_DRAFT:{risk:'LOW'},SMART_ASSIGNMENT:{risk:'MEDIUM'},STAGE_CHANGE:{risk:'MEDIUM'},ROLL_OUT_EXPERIMENT:{risk:'HIGH',neverAuto:true},ROLLBACK_EXPERIMENT:{risk:'HIGH',neverAuto:true},DELETE_LEAD:{risk:'CRITICAL',neverAuto:true},SEND_EXTERNAL_MESSAGE:{risk:'HIGH',neverAuto:true},CONTRACT_CHANGE:{risk:'CRITICAL',neverAuto:true},FINANCIAL_CHANGE:{risk:'CRITICAL',neverAuto:true},USER_PERMISSION_CHANGE:{risk:'CRITICAL',neverAuto:true}};export function getAiGovernance(action:string,trust:TrustLevel){const p=policies[action]||{risk:'HIGH' as AiRisk,neverAuto:true};let mode:AiMode='MANAGER_APPROVAL';if(p.risk==='CRITICAL')mode='BLOCKED';else if(p.neverAuto)mode='MANAGER_APPROVAL';else if(trust==='HIGH_TRUST')mode=p.risk==='LOW'?'AUTO':p.risk==='MEDIUM'?'SUGGEST':'MANAGER_APPROVAL';else if(trust==='MODERATE_TRUST')mode=p.risk==='LOW'?'SUGGEST':'MANAGER_APPROVAL';else if(trust==='LOW_TRUST')mode='MANAGER_APPROVAL';else mode=p.risk==='LOW'?'SUGGEST':'BLOCKED';return{action,risk:p.risk,mode,requiresHuman:mode==='MANAGER_APPROVAL'||mode==='BLOCKED',reason:mode==='AUTO'?'اقدام کم‌ریسک و Trust بالا است.':mode==='SUGGEST'?'هوش فقط پیشنهاد می‌دهد و اجرای نهایی در جریان کاری انجام می‌شود.':mode==='MANAGER_APPROVAL'?'اجرای این اقدام نیازمند تأیید صریح مدیر است.':'اجرای خودکار این اقدام توسط AI مجاز نیست.'}}export function listAiGovernance(trust:TrustLevel){return Object.keys(policies).map(x=>getAiGovernance(x,trust))}
