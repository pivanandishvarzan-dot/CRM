import type{DataActor}from'@/lib/data-scope';import{getExperimentAnalytics}from'./assignment-experiment-repository';import{getExperimentHealth}from'./experiment-health-repository';export async function getExperimentDecision(actor:DataActor){const[analytics,health]=await Promise.all([getExperimentAnalytics(actor),getExperimentHealth(actor)]);if(!analytics.experiment)return{experiment:null,decision:'NO_EXPERIMENT',title:'آزمایش فعالی وجود ندارد',reason:'برای تصمیم‌گیری ابتدا یک آزمایش تخصیص فعال کنید.',analytics,health};let decision='CONTINUE',title='آزمایش را ادامه دهید',reason='هنوز شواهد کافی برای تغییر سهم وجود ندارد.';if(health.status==='CRITICAL'){decision='PAUSE';title='ابتدا مشکل کیفیت آزمایش را برطرف کنید';reason='Health Score آزمایش بحرانی است و تصمیم آماری در این وضعیت قابل اتکا نیست.'}else if(analytics.rollback?.eligible){decision='ROLLBACK';title=`پیشنهاد Rollback به ${analytics.rollback.target}٪`;reason=analytics.rollback.reason}else if(health.status==='HEALTHY'&&analytics.rollout?.eligible&&analytics.rollout?.next){decision='ROLLOUT';title=`پیشنهاد افزایش Smart به ${analytics.rollout.next}٪`;reason=analytics.rollout.reason}else if(analytics.statistics?.verdict==='CONTROL_WIN'&&analytics.statistics?.ready){decision='STOP';title='توقف یا بازطراحی Smart Assignment';reason='گروه کنترل با شواهد آماری کافی عملکرد بهتری دارد.'}else if(!analytics.statistics?.ready){decision='COLLECT_MORE';title='نمونه بیشتری جمع کنید';reason='حجم نمونه هنوز برای نتیجه‌گیری نهایی کافی نیست.'}else if(health.status==='WARNING'){decision='FIX_HEALTH';title='آزمایش را ادامه دهید، اما Guardrailها را اصلاح کنید';reason='کیفیت آزمایش هشدار دارد و بهتر است قبل از Rollout اصلاح شود.'}return{experiment:analytics.experiment,decision,title,reason,analytics,health}}
