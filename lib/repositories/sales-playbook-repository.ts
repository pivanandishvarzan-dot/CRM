import{isDemoMode}from'@/lib/prisma';import type{DataActor}from'@/lib/data-scope';import{getWinLossInsights}from'./win-loss-insights-repository';const base:any={PRICE_BUDGET:{title:'مدیریت اعتراض قیمت و بودجه',actions:['بودجه واقعی و سقف انعطاف مشتری را دوباره تأیید کن.','سه گزینه با Trade-off روشن قیمت/منطقه/ویژگی ارائه کن.','قبل از تخفیف، ارزش و هزینه فرصت را توضیح بده.']},COMPETITOR:{title:'مقابله با خروج به سمت رقیب',actions:['معیار تصمیم مشتری را دقیق ثبت کن.','تفاوت پیشنهاد را روی همان معیارها نشان بده.','Next Step کوتاه و مشخص قبل از قطع ارتباط بگیر.']},FOLLOWUP:{title:'Follow-up Discipline',actions:['برای هر تعامل Next Step زمان‌دار ثبت کن.','Lead بدون پاسخ را بر اساس Urgency اولویت‌بندی کن.','پس از بازدید همان روز Follow-up انجام بده.']},NO_MATCH:{title:'بهبود Property Match',actions:['Must-have و Nice-to-have را جدا کن.','دامنه منطقه یا بودجه را با اجازه مشتری بازتنظیم کن.','برای Match جدید Alert فعال نگه دار.']},FINANCING:{title:'رفع مانع تأمین مالی',actions:['توان پرداخت و زمان تأمین وجه را زودتر Qualify کن.','سناریوی پرداخت جایگزین را با مشتری مرور کن.','ریسک مالی را قبل از Negotiation نهایی روشن کن.']},TIMING:{title:'مدیریت زمان خرید',actions:['تاریخ تصمیم واقعی مشتری را ثبت کن.','Follow-up را با Trigger زمانی مشتری هماهنگ کن.','Lead زودهنگام را از Pipeline داغ جدا نگه دار.']}};export async function getSalesPlaybook(actor:DataActor){if(isDemoMode)return{summary:{modules:0},modules:[]};const w=await getWinLossInsights(actor),total=Math.max(1,w.summary.lost),modules=w.reasons.map((r:any)=>{const p=base[r.reason]||{title:'کاهش Lostهای سایر',actions:['Lost Note را دقیق‌تر ثبت کن.','مانع اصلی تصمیم را قبل از بستن پرونده مشخص کن.','الگوی تکرارشونده را در جلسه تیم مرور کن.']};return{reason:r.reason,title:p.title,count:r.count,share:Math.round(r.count/total*1000)/10,priority:r.count/total>=.3?'HIGH':r.count/total>=.15?'MEDIUM':'LOW',actions:p.actions,metric:r.reason==='FOLLOWUP'?'SLA Met Rate':r.reason==='NO_MATCH'?'Match-to-Visit Rate':'Win Rate'}}).sort((a:any,b:any)=>b.count-a.count);return{summary:{modules:modules.length,highPriority:modules.filter((x:any)=>x.priority==='HIGH').length,winRate:w.summary.winRate,structuredLossRate:w.summary.structuredLossRate},modules,benchmark:w.agents[0]||null}}
