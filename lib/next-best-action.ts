type Input={status:string;leadScore:number;riskScore:number;urgency:number;nextFollowupAt:Date|null;lastCommunicationAt:Date|null;positiveMatches:number;negativeMatches:number;hasContract:boolean};type Action={key:string;title:string;reason:string;priority:number;hrefSuffix?:string};export function getNextBestActions(x:Input,now=new Date()){const out:Action[]=[];const days=(d:Date|null)=>d?Math.max(0,(now.getTime()-d.getTime())/864e5):999;if(x.nextFollowupAt&&x.nextFollowupAt<now)out.push({key:'OVERDUE_FOLLOWUP',title:'پیگیری عقب‌افتاده را همین امروز انجام دهید',reason:'برای این مشتری یک Follow-up گذشته از موعد وجود دارد.',priority:100});if(x.riskScore>=70)out.push({key:'RISK_RESCUE',title:'تماس نجات معامله',reason:'Risk Score پرونده بحرانی است و ادامه تأخیر احتمال از دست رفتن معامله را بالا می‌برد.',priority:96});if(days(x.lastCommunicationAt)>3&&x.leadScore>=60)out.push({key:'REENGAGE',title:'تماس یا پیام مستقیم با مشتری',reason:'Lead گرم/داغ است اما بیش از ۳ روز ارتباط تازه‌ای ثبت نشده.',priority:90,hrefSuffix:'communications'});if((x.status==='QUALIFIED'||x.status==='MATCHED')&&x.positiveMatches===0)out.push({key:'SEND_MATCHES',title:'چند ملک مناسب جدید پیشنهاد دهید',reason:'نیازسنجی انجام شده اما هنوز Match مثبت ثبت نشده است.',priority:82});if(x.negativeMatches>=2&&x.negativeMatches>x.positiveMatches)out.push({key:'REQUALIFY',title:'نیازسنجی مشتری را بازبینی کنید',reason:'چند پیشنهاد قبلی بازخورد منفی گرفته‌اند؛ معیارهای جست‌وجو احتمالاً نیاز به اصلاح دارند.',priority:88});if(x.status==='MATCHED'&&x.positiveMatches>0)out.push({key:'BOOK_VISIT',title:'برای ملک مناسب بازدید هماهنگ کنید',reason:'حداقل یک Match مثبت وجود دارد و پرونده آماده حرکت به مرحله بازدید است.',priority:86});if(x.status==='VISIT')out.push({key:'VISIT_FOLLOWUP',title:'نتیجه بازدید را پیگیری کنید',reason:'پرونده در مرحله بازدید است و تصمیم مشتری باید ثبت شود.',priority:92});if(x.status==='NEGOTIATION')out.push({key:'NEGOTIATION',title:'مانع اصلی مذاکره را مشخص و حل کنید',reason:'پرونده در مذاکره است؛ بهترین اقدام، مشخص‌کردن مانع قیمت/شرایط و پیشنهاد بعدی است.',priority:94});if(x.status==='CONTRACT'&&!x.hasContract)out.push({key:'CLOSE_CONTRACT',title:'مدارک و عقد قرارداد را نهایی کنید',reason:'Stage روی قرارداد است اما قرارداد نهایی هنوز ثبت نشده.',priority:98});if(x.status==='LEAD'||x.status==='CONTACTED')out.push({key:'QUALIFY',title:'نیازسنجی کامل انجام دهید',reason:'پرونده هنوز در مراحل ابتدایی Pipeline است و اطلاعات کامل نیاز مشتری ارزش اقدام بعدی را بالا می‌برد.',priority:70});if(!out.length)out.push({key:'MAINTAIN',title:'پیگیری برنامه‌ریزی‌شده را حفظ کنید',reason:'در حال حاضر ریسک یا مانع فوری شناسایی نشده است.',priority:40});return out.sort((a,b)=>b.priority-a.priority).slice(0,3)}