import {properties} from '@/lib/demo-data';import {Badge} from '@/components/ui';import {ArrowRight,MapPin,Maximize2,BedDouble,Building,Calendar,User,Phone,Edit3} from 'lucide-react';import Link from 'next/link';import {notFound} from 'next/navigation';
export default function Page({params}:{params:{id:string}}){const p=properties.find(x=>x.id===Number(params.id));if(!p)notFound();return <><Link href="/properties" className="mb-5 inline-flex items-center gap-2 text-sm text-slate-500"><ArrowRight size={17}/>بازگشت به ملک‌ها</Link><div className="mb-5 flex items-start justify-between"><div><div className="flex items-center gap-3"><h1 className="text-2xl font-bold">{p.title}</h1><Badge>{p.status}</Badge></div><p className="mt-2 flex items-center gap-1 text-sm text-slate-500"><MapPin size={16}/>{p.district}، {p.city} · {p.code}</p></div><button className="btn-secondary"><Edit3 size={17}/>ویرایش</button></div><div className="grid gap-5 lg:grid-cols-3"><div className="space-y-5 lg:col-span-2"><img className="card h-[360px] w-full object-cover" src={p.image} alt={p.title}/><div className="card p-6"><h2 className="mb-5 font-bold">مشخصات ملک</h2><div className="grid grid-cols-2 gap-5 md:grid-cols-4">{[[Maximize2,'متراژ',`${p.area} متر`],[BedDouble,'اتاق',`${p.rooms} خواب`],[Building,'طبقه',`${p.floor}`],[Calendar,'سن بنا',`${p.age} سال`]].map(([I,l,v]:any)=><div key={l} className="rounded-xl bg-slate-50 p-4"><I className="mb-3 text-brand-600"/><small className="text-slate-500">{l}</small><b className="block">{v}</b></div>)}</div><h3 className="mb-3 mt-7 font-bold">امکانات</h3><div className="flex flex-wrap gap-2">{p.features.map(x=><Badge key={x} tone="gray">✓ {x}</Badge>)}</div></div></div><aside className="space-y-5"><div className="card p-6"><small className="text-slate-500">قیمت {p.deal}</small><b className="mt-2 block text-2xl text-brand-700">{p.price} میلیارد تومان</b><hr className="my-5"/><h3 className="font-bold">اطلاعات مالک</h3><div className="mt-4 flex gap-3"><span className="rounded-full bg-brand-50 p-3 text-brand-700"><User/></span><div><b>{p.owner}</b><p className="text-xs text-slate-500">مالک حقیقی</p></div></div><button className="btn-primary mt-4 w-full"><Phone size={17}/>تماس با مالک</button></div><div className="card p-6"><h3 className="font-bold">مشاور مسئول</h3><p className="mt-3 text-sm">{p.agent}</p><p className="mt-1 text-xs text-slate-500">آخرین پیگیری: دو روز پیش</p></div></aside></div></>}
