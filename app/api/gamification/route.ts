import{NextResponse}from'next/server';import{requireUser}from'@/lib/authz';import{getLeaderboard}from'@/lib/repositories/gamification-repository';import{apiError}from'@/lib/api-validation';export async function GET(request:Request){try{const actor=await requireUser();const mode=new URL(request.url).searchParams.get('mode')==='month'?'month':'week';return NextResponse.json({data:await getLeaderboard(actor,mode),mode})}catch(e){const out=apiError(e,'دریافت جدول امتیازات انجام نشد.');return NextResponse.json({error:out.message},{status:out.status})}}
