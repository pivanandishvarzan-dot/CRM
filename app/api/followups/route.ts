import {NextResponse} from 'next/server';import {createFollowup,listFollowups} from '@/lib/repositories/followups';export const dynamic='force-dynamic';
export async function GET(){try{return NextResponse.json({data:await listFollowups()})}catch(e){console.error(e);return NextResponse.json({error:'خطا در دریافت پیگیری‌ها'},{status:500})}}
export async function POST(req:Request){try{const b=await req.json();if(!b.title||!b.type||!b.scheduledAt)return NextResponse.json({error:'عنوان، نوع و زمان پیگیری الزامی است'},{status:400});const date=new Date(b.scheduledAt);if(Number.isNaN(date.getTime()))return NextResponse.json({error:'زمان پیگیری معتبر نیست'},{status:400});return NextResponse.json({data:await createFollowup({...b,priority:Number(b.priority)||1})},{status:201})}catch(e){console.error(e);return NextResponse.json({error:'خطا در ثبت پیگیری'},{status:500})}}
