import {NextResponse}from'next/server';import{requireRole,requireUser}from'@/lib/authz';import{getMatchingConfig,saveMatchingConfig}from'@/lib/matching/matching-config';import{apiError}from'@/lib/api-validation';
export async function GET(){try{return NextResponse.json({data:await getMatchingConfig(await requireUser())})}catch(e){const out=apiError(e,'دریافت تنظیمات Matching انجام نشد.');return NextResponse.json({error:out.message},{status:out.status})}}
export async function PATCH(request:Request){try{const actor=await requireRole(['SYSTEM_ADMIN','AGENCY_MANAGER']);const body=await request.json();return NextResponse.json({data:await saveMatchingConfig(actor,body)})}catch(e){const out=apiError(e,'ذخیره تنظیمات Matching انجام نشد.');return NextResponse.json({error:out.message},{status:out.status})}}
