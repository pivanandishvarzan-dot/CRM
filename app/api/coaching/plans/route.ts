import{NextResponse}from'next/server';import{requireManager}from'@/lib/authz';import{ensureWeeklyPlans}from'@/lib/repositories/improvement-plan-repository';import{apiError}from'@/lib/api-validation';export async function GET(){try{return NextResponse.json({data:await ensureWeeklyPlans(await requireManager())})}catch(e){const out=apiError(e,'ساخت برنامه بهبود هفتگی انجام نشد.');return NextResponse.json({error:out.message},{status:out.status})}}
