'use client';import{useEffect,useState}from'react';import{Badge}from'./ui';const pct=(x:number)=>`${Number(x||0).toLocaleString('fa-IR')}٪`,fa=(x:string)=>new Intl.DateTimeFormat('fa-IR-u-ca-persian',{day:'numeric',month:'long',year:'numeric'}).format(new Date(x));export default function LeadAssignmentAnalytics(){const[d,setD]=useState<any>(null),[e,setE]=useState('');useEffect(()=>{fetch('/api/management/lead-assignment/analytics').then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error);setD(j.data)}).catch(x=>setE(x.message))},[]);if(e)return <div className="card p-6 text-red-700">{e}</div>;if(!d)return <div className="card p-6">در حال تحلیل تخصیص‌ها...</div>;return <><div className="mb-6"><p className="text-sm text-slate-500">{fa(new Date().toISOString())}</p><h1 className="mt-1 text-2xl font-bold">Lead Assignment Analytics</h1><p className="mt-2 text-sm text-slate-500">اثر واقعی Smart Assignment بر پیشرفت لید و قرارداد.</p></div><div className="grid gap-3 md:grid-cols-5"><div className="card p-4"><small>تخصیص</small><b className="block text-2xl">{d.summary.assignments.toLocaleString('fa-IR')}</b></div><div className="card p-4"><small>ارزیابی‌شده</small><b className="block text-2xl">{d.summary.evaluated.toLocaleString('fa-IR')}</b></div><div className="card p-4"><small>Success</small><b className="block text-2xl">{pct(d.summary.successRate)}</b></div><div className="card p-4"><small>انتخاب رتبه ۱</small><b className="block text-2xl">{pct(d.summary.topRankShare)}</b></div><div className="card p-4"><small>قرارداد</small><b className="block text-2xl">{d.summary.contracts.toLocaleString('fa-IR')}</b></div></div><div className="mt-5 grid gap-5 xl:grid-cols-2"><section className="card overflow-x-auto p-5"><h2 className="font-bold">نتیجه بر اساس مشاور</h2><table className="mt-4 w-full text-sm"><thead><tr><th className="text-right">مشاور</th><th>ارزیابی</th><th>Success</th><th>قرارداد</th></tr></thead><tbody>{d.agents.map((x:any)=><tr key={x.id} className="border-t"><td className="p-3 font-medium">{x.name}</td><td>{x.evaluated.toLocaleString('fa-IR')}</td><td><Badge tone={x.successRate>=60?'green':x.successRate>=40?'amber':'red'}>{pct(x.successRate)}</Badge></td><td>{x.contracts.toLocaleString('fa-IR')}</td></tr>)}</tbody></table></section><section className="card p-5"><h2 className="font-bold">کیفیت رتبه‌های پیشنهادی</h2><div className="mt-4 space-y-3">{d.ranks.map((x:any)=><div key={x.rank} className="rounded-xl border p-3"><div className="flex justify-between"><b>رتبه {String(x.rank).replace('MANUAL','دستی')}</b><span>{pct(x.successRate)}</span></div><p className="mt-1 text-xs text-slate-400">{x.evaluated.toLocaleString('fa-IR')} ارزیابی · {x.contracts.toLocaleString('fa-IR')} قرارداد</p></div>)}</div></section></div><section className="card mt-5 overflow-x-auto p-5"><h2 className="font-bold">آخرین تخصیص‌ها</h2><table className="mt-4 w-full min-w-[650px] text-sm"><thead><tr><th className="text-right">تاریخ</th><th>مشاور</th><th>رتبه</th><th>Score</th><th>Outcome</th></tr></thead><tbody>{d.recent.map((x:any)=><tr key={x.id} className="border-t"><td className="p-3">{fa(x.assignedAt)}</td><td>{x.agent}</td><td>{x.rank??'—'}</td><td>{x.rankingScore??'—'}</td><td>{x.evaluatedAt?(x.success?'موفق':'بدون پیشرفت'):'در انتظار ارزیابی'}</td></tr>)}</tbody></table></section></>}
