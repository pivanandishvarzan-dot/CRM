'use client';import{useEffect,useState}from'react';import{Badge}from'./ui';const pct=(x:number)=>`${Number(x||0).toLocaleString('fa-IR')}٪`;export default function FocusAnalyticsDashboard(){const[d,setD]=useState<any>(null),[e,setE]=useState('');useEffect(()=>{fetch('/api/action-center/focus/analytics').then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error);setD(j.data)}).catch(x=>setE(x.message))},[]);if(e)return <div className="card p-6 text-red-700">{e}</div>;if(!d)return <div className="card p-6">در حال تحلیل Focus Mode...</div>;return <><div className="mb-6"><p className="text-sm text-slate-500">{new Intl.DateTimeFormat('fa-IR-u-ca-persian',{weekday:'long',day:'numeric',month:'long',year:'numeric'}).format(new Date())}</p><h1 className="mt-1 text-2xl font-bold">Focus Session Analytics</h1><p className="mt-2 text-sm text-slate-500">رفتار ۳۰ روز اخیر تیم در صف کاری متمرکز.</p></div><div className="grid gap-3 sm:grid-cols-4"><div className="card p-4"><small>تکمیل‌شده</small><b className="mt-2 block text-2xl">{d.summary.completed.toLocaleString('fa-IR')}</b></div><div className="card p-4"><small>Completion Rate</small><b className="mt-2 block text-2xl">{pct(d.summary.completionRate)}</b></div><div className="card p-4"><small>Snooze</small><b className="mt-2 block text-2xl">{d.summary.snoozed.toLocaleString('fa-IR')}</b></div><div className="card p-4"><small>ردشده</small><b className="mt-2 block text-2xl">{d.summary.dismissed.toLocaleString('fa-IR')}</b></div></div><div className="mt-5 grid gap-5 xl:grid-cols-2"><section className="card overflow-x-auto p-5"><h2 className="font-bold">بهره‌وری کاربران</h2><table className="mt-4 w-full min-w-[560px] text-sm"><thead><tr className="text-right text-xs text-slate-500"><th className="p-2">کاربر</th><th>کل</th><th>تکمیل</th><th>Completion</th><th>Snooze</th></tr></thead><tbody>{d.users.map((x:any)=><tr key={x.id} className="border-t"><td className="p-3 font-medium">{x.name}</td><td>{x.total.toLocaleString('fa-IR')}</td><td>{x.completed.toLocaleString('fa-IR')}</td><td><Badge tone={x.completionRate>=70?'green':x.completionRate>=45?'amber':'red'}>{pct(x.completionRate)}</Badge></td><td>{pct(x.snoozeRate)}</td></tr>)}</tbody></table></section><section className="card p-5"><h2 className="font-bold">کارهایی که بیشتر عقب می‌افتند</h2><div className="mt-4 space-y-4">{d.types.map((x:any)=><div key={x.type}><div className="flex justify-between text-sm"><b>{x.type}</b><span>Snooze {pct(x.snoozeRate)}</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full bg-brand-500" style={{width:`${Math.min(100,x.snoozeRate)}%`}}/></div><p className="mt-1 text-xs text-slate-400">{x.total.toLocaleString('fa-IR')} اقدام · Completion {pct(x.completionRate)}</p></div>)}</div></section></div></>}
