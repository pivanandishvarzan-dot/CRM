'use client';import{useEffect,useState}from'react';import{Award,Flame,Trophy}from'lucide-react';import{Badge}from'./ui';type Row={id:string;name:string;rank:number;points:number;completed:number;total:number;discipline:number;conversion:number;contracts:number;contractValue:number;streakDays:number;quotaPct:number;badges:string[]};export default function GamificationCenter(){const[mode,setMode]=useState<'week'|'month'>('week'),[rows,setRows]=useState<Row[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState('');useEffect(()=>{setLoading(true);fetch(`/api/gamification?mode=${mode}`).then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error||'خطا');setRows(j.data||[])}).catch(e=>setError(e.message)).finally(()=>setLoading(false))},[mode]);return <><div className="mb-6 flex flex-wrap items-end gap-3"><div><p className="text-sm text-slate-500">{new Intl.DateTimeFormat('fa-IR-u-ca-persian',{weekday:'long',day:'numeric',month:'long',year:'numeric'}).format(new Date())}</p><h1 className="mt-1 text-2xl font-bold">رقابت تیم فروش</h1><p className="mt-2 text-sm text-slate-500">امتیاز بر اساس کیفیت عملکرد، نه فقط حجم فروش.</p></div><div className="mr-auto flex rounded-xl border bg-white p-1"><button onClick={()=>setMode('week')} className={`rounded-lg px-4 py-2 text-sm ${mode==='week'?'bg-brand-600 text-white':''}`}>هفتگی</button><button onClick={()=>setMode('month')} className={`rounded-lg px-4 py-2 text-sm ${mode==='month'?'bg-brand-600 text-white':''}`}>ماهانه</button></div></div>{error&&<div className="mb-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</div>}{loading?<div className="card p-10 text-center text-sm text-slate-500">در حال محاسبه امتیازها...</div>:<><div className="grid gap-4 lg:grid-cols-3">{rows.slice(0,3).map((x,i)=><div className="card p-5" key={x.id}><div className="flex items-center gap-3"><div className={`grid h-12 w-12 place-items-center rounded-xl ${i===0?'bg-amber-50 text-amber-600':'bg-slate-100 text-slate-600'}`}><Trophy/></div><div><small>رتبه {(i+1).toLocaleString('fa-IR')}</small><b className="block">{x.name}</b></div><b className="mr-auto text-xl text-brand-700">{x.points.toLocaleString('fa-IR')}</b></div><div className="mt-4 flex flex-wrap gap-2">{x.badges.map(b=><Badge key={b} tone="amber"><Award size={12}/>{b}</Badge>)}</div></div>)}</div><div className="card mt-5 table-wrap"><table className="data-table"><thead><tr><th>رتبه</th><th>مشاور</th><th>امتیاز</th><th>Streak</th><th>نظم پیگیری</th><th>Conversion</th><th>قرارداد</th><th>Badge</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.rank.toLocaleString('fa-IR')}</td><td><b>{x.name}</b></td><td>{x.points.toLocaleString('fa-IR')}</td><td><span className="inline-flex items-center gap-1"><Flame size={14}/>{x.streakDays.toLocaleString('fa-IR')} روز</span></td><td>{x.discipline.toLocaleString('fa-IR')}٪</td><td>{x.conversion.toLocaleString('fa-IR')}٪</td><td>{x.contracts.toLocaleString('fa-IR')}</td><td><div className="flex flex-wrap gap-1">{x.badges.slice(0,2).map(b=><span key={b} className="badge bg-slate-100 text-slate-600">{b}</span>)}</div></td></tr>)}</tbody></table></div></>}</>}
