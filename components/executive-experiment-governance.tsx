'use client';import Link from'next/link';import{useEffect,useState}from'react';import{Badge}from'./ui';export default function ExecutiveExperimentGovernance(){const[d,setD]=useState<any>(null);useEffect(()=>{fetch('/api/management/lead-assignment/experiment/decision').then(r=>r.ok?r.json():null).then(j=>setD(j?.data||null)).catch(()=>{})},[]);if(!d?.experiment||!d.governance)return null;const g=d.governance,tone=g.level==='STRONG'?'green':g.level==='WEAK'?'red':'amber',open=(g.checks||[]).filter((x:any)=>!x.passed);return <section className="card mb-5 p-6"><div className="flex flex-wrap items-start justify-between gap-4"><div><div className="flex flex-wrap items-center gap-2"><h2 className="font-bold">حاکمیت آزمایش تخصیص</h2><Badge tone={tone}>{g.level}</Badge></div><p className="mt-2 text-sm text-slate-600">{g.title} — {g.detail}</p></div><div className="text-left"><b className="text-3xl">{Number(g.score||0).toLocaleString('fa-IR')}</b><span className="text-sm text-slate-400"> / ۱۰۰</span></div></div><div className="mt-5 grid gap-3 md:grid-cols-4"><div className="rounded-xl bg-slate-50 p-4"><small className="text-slate-400">تصمیم فعلی</small><b className="mt-1 block">{d.decision}</b></div><div className="rounded-xl bg-slate-50 p-4"><small className="text-slate-400">Readiness</small><b className="mt-1 block">{Number(d.readiness?.score||0).toLocaleString('fa-IR')} / ۱۰۰</b></div><div className="rounded-xl bg-slate-50 p-4"><small className="text-slate-400">Risk</small><b className="mt-1 block">{Number(d.risk?.score||0).toLocaleString('fa-IR')} / ۱۰۰</b></div><div className="rounded-xl bg-slate-50 p-4"><small className="text-slate-400">کنترل‌های باز</small><b className="mt-1 block">{open.length.toLocaleString('fa-IR')}</b></div></div>{open.length>0&&<p className="mt-4 text-sm text-amber-700">نیازمند توجه مدیر: {open.map((x:any)=>x.label).join('، ')}</p>}<div className="mt-4"><Link href="/management/lead-assignment/experiment/decision" className="text-sm font-semibold text-sky-700">باز کردن مرکز تصمیم آزمایش ←</Link></div></section>}
