'use client';import{useEffect,useState}from'react';import Link from'next/link';import{Badge}from'./ui';const n=(x:number)=>Number(x||0).toLocaleString('fa-IR');export default function ManagerMorningBrief(){const[d,setD]=useState<any>(null),[e,setE]=useState('');useEffect(()=>{fetch('/api/management/morning-brief').then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error);setD(j.data)}).catch(x=>setE(x.message))},[]);if(e)return <div className="card p-6 text-red-700">{e}</div>;if(!d)return <div className="card p-6">در حال آماده‌سازی Morning Brief...</div>;return <><div className="mb-6"><p className="text-sm text-slate-500">{new Intl.DateTimeFormat('fa-IR-u-ca-persian',{weekday:'long',day:'numeric',month:'long',year:'numeric'}).format(new Date())}</p><h1 className="mt-1 text-2xl font-bold">Manager Morning Brief</h1><p className="mt-2 text-sm text-slate-500">یک صفحه برای اینکه مدیر بداند امروز قبل از هر چیز باید کجا مداخله کند.</p></div><div className="grid gap-3 sm:grid-cols-5">{[['هشدار',d.summary.alerts],['SLA Breached',d.summary.slaBreached],['Critical Aging',d.summary.pipelineCritical],['Overloaded',d.summary.overloadedAgents],['Execution Score',`${n(d.summary.executionScore)}/100`]].map(([a,b]:any)=><div key={a} className="card p-4"><small>{a}</small><b className="block text-xl">{typeof b==='number'?n(b):b}</b></div>)}</div><section className="card mt-5 p-5"><h2 className="font-bold">اولویت‌های امروز</h2><div className="mt-4 space-y-3">{d.priorities.map((x:any,i:number)=><div key={i} className="rounded-xl border p-4"><div className="flex flex-wrap justify-between gap-3"><div><Badge tone={x.severity==='CRITICAL'?'red':x.severity==='HIGH'?'amber':'green'}>{x.severity}</Badge><b className="mt-2 block">{x.title}</b><p className="mt-1 text-xs text-slate-400">{x.area}</p></div><Link href={x.href} className="btn-secondary text-xs">باز کردن</Link></div><p className="mt-3 text-sm text-slate-600">{x.action}</p></div>)}{!d.priorities.length&&<div className="rounded-xl bg-emerald-50 p-5 text-sm text-emerald-800">هشدار مدیریتی مهمی برای امروز شناسایی نشده است.</div>}</div></section><p className="mt-4 text-xs text-slate-400">این Brief از SLA، Pipeline Aging، ظرفیت تیم و برنامه بهبود فروش به‌صورت هم‌زمان ساخته می‌شود.</p></>}
