'use client';import{useEffect,useState}from'react';import{Badge}from'./ui';const n=(x:number)=>Number(x||0).toLocaleString('fa-IR',{maximumFractionDigits:1}),m=(x:number)=>new Intl.NumberFormat('fa-IR',{maximumFractionDigits:0}).format(Number(x||0));export default function SalesTargetGapDashboard(){const[d,setD]=useState<any>(null),[e,setE]=useState('');useEffect(()=>{fetch('/api/management/sales-target-gap').then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error);setD(j.data)}).catch(x=>setE(x.message))},[]);if(e)return <div className="card p-6 text-red-700">{e}</div>;if(!d)return <div className="card p-6">در حال محاسبه Gap-to-Goal...</div>;const s=d.summary;return <><div className="mb-6"><p className="text-sm text-slate-500">هدف ماه {d.monthKey}</p><h1 className="mt-1 text-2xl font-bold">Sales Target & Gap-to-Goal</h1><p className="mt-2 text-sm text-slate-500">فاصله Forecast تا هدف و حجم تقریبی قرارداد و Lead لازم برای جبران آن.</p></div><section className="card p-6"><div className="flex flex-wrap justify-between gap-5"><div><Badge tone={s.attainment>=100?'green':s.attainment>=75?'amber':'red'}>{n(s.attainment)}٪ هدف</Badge><h2 className="mt-3 text-xl font-bold">{s.gap>0?`Gap فعلی: ${m(s.gap)}`:'Forecast فعلی هدف را پوشش می‌دهد.'}</h2><p className="mt-2 text-sm text-slate-500">Confidence Forecast: {s.confidence}</p></div><div className="text-left"><small>Forecast / Target</small><b className="block text-2xl">{m(s.forecast)} / {m(s.target)}</b></div></div><div className="mt-5 h-3 overflow-hidden rounded-full bg-slate-100"><div className="h-full bg-slate-800" style={{width:`${Math.min(100,s.attainment)}%`}}/></div></section><div className="mt-5 grid gap-3 sm:grid-cols-5">{[['Target',m(s.target)],['Forecast',m(s.forecast)],['Booked',m(s.booked)],['قرارداد لازم',n(s.dealsNeeded)],['Lead لازم',n(s.leadsNeeded)]].map(([a,b])=><div key={a} className="card p-4"><small>{a}</small><b className="block text-xl">{b}</b></div>)}</div><section className="card mt-5 overflow-x-auto p-5"><h2 className="font-bold">Gap by Agent</h2><table className="mt-4 w-full min-w-[850px] text-sm"><thead><tr><th className="text-right">مشاور</th><th>هدف</th><th>Forecast</th><th>تحقق</th><th>Gap</th><th>قرارداد لازم</th><th>Lead لازم</th></tr></thead><tbody>{d.agents.map((x:any)=><tr key={x.userId} className="border-t"><td className="p-3 font-medium">{x.name}</td><td>{m(x.target)}</td><td>{m(x.forecast)}</td><td><Badge tone={x.attainment>=100?'green':x.attainment>=75?'amber':'red'}>{n(x.attainment)}٪</Badge></td><td>{m(x.gap)}</td><td>{n(x.dealsNeeded)}</td><td>{n(x.leadsNeeded)}</td></tr>)}</tbody></table>{!d.agents.length&&<p className="mt-4 text-sm text-slate-500">برای این ماه SalesQuota ثبت نشده است.</p>}</section><p className="mt-4 text-xs text-slate-400">Lead و قرارداد لازم بر اساس نرخ تبدیل Pipeline و Avg Deal فعلی تخمین زده می‌شوند و مقدار قطعی نیستند.</p></>}
