'use client';import{useEffect,useState}from'react';import Link from'next/link';import{Badge}from'./ui';const pct=(x:number)=>`${Number(x||0).toLocaleString('fa-IR')}٪`;export default function AiTrustCenter(){const[d,setD]=useState<any>(null),[e,setE]=useState('');useEffect(()=>{fetch('/api/management/ai-trust').then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error);setD(j.data)}).catch(x=>setE(x.message))},[]);if(e)return <div className="card p-6 text-red-700">{e}</div>;if(!d)return <div className="card p-6">در حال محاسبه Trust Score...</div>;const tone=d.status==='HIGH_TRUST'?'green':d.status==='MODERATE_TRUST'?'amber':'red';return <><div className="mb-6"><p className="text-sm text-slate-500">{new Intl.DateTimeFormat('fa-IR-u-ca-persian',{weekday:'long',day:'numeric',month:'long',year:'numeric'}).format(new Date())}</p><h1 className="mt-1 text-2xl font-bold">AI Trust Center</h1><p className="mt-2 text-sm text-slate-500">نمای مرکزی میزان قابل‌اعتماد بودن موتورهای تصمیم‌یار CRM.</p></div><section className="card p-6"><div className="flex flex-wrap items-start justify-between gap-5"><div><Badge tone={tone}>{d.status}</Badge><h2 className="mt-3 text-xl font-bold">Trust Score: {d.score.toLocaleString('fa-IR')} / ۱۰۰</h2><p className="mt-2 max-w-3xl text-sm text-slate-600">{d.recommendation}</p></div><div className="text-center"><div className="grid h-24 w-24 place-items-center rounded-full border-8 border-slate-100 text-3xl font-bold">{d.score.toLocaleString('fa-IR')}</div></div></div></section><div className="mt-5 grid gap-3 md:grid-cols-4">{d.signals.map((x:any)=><div key={x.key} className="card p-4"><div className="flex justify-between gap-2"><small>{x.label}</small><Badge tone={x.status==='GOOD'?'green':x.status==='WARN'?'amber':'red'}>{x.status}</Badge></div><b className="mt-2 block text-2xl">{x.key==='drift'?Number(x.value).toLocaleString('fa-IR'):pct(x.value)}</b></div>)}</div><div className="mt-5 grid gap-4 lg:grid-cols-2"><Link href="/management/decisions" className="card p-5"><b>Decision Memory</b><p className="mt-2 text-sm text-slate-500">{d.decisions.summary.total.toLocaleString('fa-IR')} پیشنهاد ثبت‌شده · Accuracy {pct(d.decisions.summary.accuracy)}</p></Link><Link href="/management/decisions/calibration" className="card p-5"><b>Confidence Calibration</b><p className="mt-2 text-sm text-slate-500">Calibration Error: {pct(d.calibration.summary.calibrationError)}</p></Link><Link href="/management/decisions/drift" className="card p-5"><b>Decision Drift</b><p className="mt-2 text-sm text-slate-500">وضعیت: {d.drift.status} · Drift Score {Number(d.drift.driftScore||0).toLocaleString('fa-IR')}</p></Link><Link href="/management/lead-assignment/experiment/health" className="card p-5"><b>Experiment Health</b><p className="mt-2 text-sm text-slate-500">Health Score: {Number(d.experiment.score||0).toLocaleString('fa-IR')} · {d.experiment.status}</p></Link></div></>}
