'use client';
import {useMemo,useState} from 'react';import Link from 'next/link';import {Grid2X2,List,Plus,Search,SlidersHorizontal,MapPin,BedDouble,Maximize2,Trash2} from 'lucide-react';import {properties as initial} from '@/lib/demo-data';import {Badge,Empty,Modal} from './ui';
export default function PropertiesView(){const[data,setData]=useState(initial);const[q,setQ]=useState('');const[deal,setDeal]=useState('همه');const[view,setView]=useState<'grid'|'list'>('grid');const[modal,setModal]=useState(false);const filtered=useMemo(()=>data.filter(p=>(p.title+p.district+p.code).includes(q)&&(deal==='همه'||p.deal===deal)),[data,q,deal]);function add(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);setData([{...initial[0],id:Date.now(),title:String(f.get('title')),code:'MLK-جدید',price:Number(f.get('price'))||0,status:'فعال'},...data]);setModal(false)}function del(id:number){if(confirm('از حذف این ملک مطمئن هستید؟'))setData(data.filter(x=>x.id!==id))}return <><Header title="مدیریت ملک‌ها" sub={`${data.length} ملک ثبت‌شده در سامانه`} action={()=>setModal(true)} label="ثبت ملک جدید"/><div className="card mb-5 flex flex-wrap items-center gap-3 p-4"><div className="relative min-w-[220px] flex-1"><Search className="absolute right-3 top-3 text-slate-400" size={18}/><input value={q} onChange={e=>setQ(e.target.value)} className="input pr-10" placeholder="جست‌وجو با عنوان، کد یا منطقه..."/></div><select value={deal} onChange={e=>setDeal(e.target.value)} className="input w-40"><option>همه</option><option>فروش</option><option>اجاره</option><option>رهن و اجاره</option></select><button className="btn-secondary"><SlidersHorizontal size={17}/>فیلتر پیشرفته</button><div className="flex rounded-xl border p-1"><button aria-label="نمای کارت" onClick={()=>setView('grid')} className={`rounded-lg p-2 ${view==='grid'?'bg-brand-50 text-brand-700':''}`}><Grid2X2 size={18}/></button><button aria-label="نمای فهرست" onClick={()=>setView('list')} className={`rounded-lg p-2 ${view==='list'?'bg-brand-50 text-brand-700':''}`}><List size={18}/></button></div></div>{!filtered.length?<div className="card"><Empty/></div>:view==='grid'?<div className="grid gap-5 sm:grid-cols-2 xl:grid-cols-3">{filtered.map(p=><div className="card group overflow-hidden" key={p.id}><div className="relative h-48"><img src={p.image} alt={p.title} className="h-full w-full object-cover"/><span className="absolute right-3 top-3"><Badge tone={p.status==='فعال'?'green':'amber'}>{p.status}</Badge></span><button onClick={()=>del(p.id)} aria-label="حذف" className="absolute left-3 top-3 rounded-lg bg-white/90 p-2 text-red-500 opacity-0 transition group-hover:opacity-100"><Trash2 size={17}/></button></div><div className="p-5"><div className="flex justify-between gap-2"><Link href={`/properties/${p.id}`} className="font-bold hover:text-brand-700">{p.title}</Link><small className="text-slate-400">{p.code}</small></div><p className="mt-2 flex items-center gap-1 text-xs text-slate-500"><MapPin size={14}/>{p.district}، {p.city}</p><div className="my-4 flex gap-4 border-y py-3 text-xs text-slate-600"><span className="flex gap-1"><Maximize2 size={15}/>{p.area} متر</span><span className="flex gap-1"><BedDouble size={16}/>{p.rooms} خواب</span><span>طبقه {p.floor}</span></div><div className="flex items-center justify-between"><div><small className="text-slate-500">{p.deal}</small><b className="block text-brand-700">{p.price} میلیارد تومان</b></div><span className="text-xs text-slate-500">{p.agent}</span></div></div></div>)}</div>:<div className="card table-wrap"><table className="data-table"><thead><tr><th>ملک</th><th>نوع معامله</th><th>منطقه</th><th>متراژ</th><th>قیمت</th><th>وضعیت</th><th/></tr></thead><tbody>{filtered.map(p=><tr key={p.id}><td><Link className="font-bold hover:text-brand-700" href={`/properties/${p.id}`}>{p.title}<small className="block font-normal text-slate-400">{p.code}</small></Link></td><td>{p.deal}</td><td>{p.district}</td><td>{p.area} متر</td><td>{p.price} میلیارد</td><td><Badge>{p.status}</Badge></td><td><button onClick={()=>del(p.id)} className="text-red-500"><Trash2 size={17}/></button></td></tr>)}</tbody></table></div>}<Modal open={modal} onClose={()=>setModal(false)} title="ثبت ملک جدید"><form onSubmit={add} className="grid gap-4 md:grid-cols-2"><div className="md:col-span-2"><label className="label">عنوان ملک</label><input required name="title" className="input" placeholder="مثلاً آپارتمان سه‌خوابه پاسداران"/></div>{['نوع ملک','نوع معامله','شهر','منطقه'].map(x=><div key={x}><label className="label">{x}</label><select className="input"><option>انتخاب کنید</option><option>آپارتمان</option><option>فروش</option></select></div>)}<div><label className="label">قیمت (میلیارد تومان)</label><input name="price" type="number" className="input"/></div><div><label className="label">متراژ</label><input type="number" className="input"/></div><div className="md:col-span-2 flex justify-end gap-2 pt-3"><button type="button" onClick={()=>setModal(false)} className="btn-secondary">انصراف</button><button className="btn-primary">ذخیره ملک</button></div></form></Modal></>}
export function Header({title,sub,action,label}:{title:string;sub:string;action?:()=>void;label?:string}){return <div className="mb-6 flex items-center justify-between"><div><h1 className="text-2xl font-bold">{title}</h1><p className="mt-1 text-sm text-slate-500">{sub}</p></div>{action&&<button onClick={action} className="btn-primary"><Plus size={18}/>{label}</button>}</div>}
