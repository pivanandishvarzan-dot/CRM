'use client';import{useEffect,useState}from'react';import{Badge}from'./ui';const n=(x:number)=>Number(x||0).toLocaleString('fa-IR',{maximumFractionDigits:1});export default function SalesPlaybookScorecard(){const[d,setD]=useState<any>(null),[e,setE]=useState('');useEffect(()=>{fetch('/api/management/sales-playbook/scorecard').then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error);setD(j.data)}).catch(x=>setE(x.message))},[]);if(e)return <div className="card p-6 text-red-700">{e}</div>;if(!d)return <div className="card p-6">در حال محاسبه Scorecard...</div>;return <><div className="mb-6"><p className="text-sm text-slate-500">۳۰ روز اخیر</p><h1 className="mt-1 text-2xl font-bold">Sales Playbook Execution Scorecard</h1><p className="mt-2 text-sm text-slate-500">بررسی می‌کند Playbook فقط نوشته شده یا رفتار و نتیجه فروش واقعاً در حال بهترشدن است.</p></div><section className="card p-6"><div className="flex justify-between gap-4"><div><Badge tone={d.summary.status==='STRONG'?'green':d.summary.status==='IMPROVING'?'amber':'red'}>{d.summary.status}</Badge><h2 className="mt-3 text-xl font-bold">Execution Score: {n(d.summary.score)} / ۱۰۰</h2><p className="mt-2 text-sm text-slate-500">{n(d.summary.activeModules)} ماژول فعال · {n(d.summary.highPriorityModules)} ماژول High Priority</p></div></div></section><div className="mt-5 grid gap-4 md:grid-cols-3">{d.metrics.map((x:any)=><section key={x.key} className="card p-5"><div className="flex justify-between"><b>{x.label}</b><Badge tone={x.status==='ON_TRACK'?'green':x.status==='WATCH'?'amber':'red'}>{x.status}</Badge></div><b className="mt-4 block text-3xl">{n(x.value)}٪</b><p className="mt-1 text-xs text-slate-500">Target: {n(x.target)}٪ · Score {n(x.score)}/100</p><div className="mt-4 h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full bg-slate-800" style={{width:`${Math.min(100,x.score)}%`}}/></div></section>)}</div><section className="card mt-5 p-5"><h2 className="font-bold">Focus for Next Review</h2><div className="mt-3 space-y-2">{d.focus.map((x:any)=><div key={x.key} className="rounded-xl border p-4"><b>{x.label}</b><p className="mt-1 text-sm text-slate-500">وضعیت {x.status} است؛ مقدار فعلی {n(x.value)}٪ در برابر Target {n(x.target)}٪.</p></div>)}{!d.focus.length&&<p className="text-sm text-emerald-700">تمام شاخص‌های اجرای Playbook روی Target یا بالاتر هستند.</p>}</div></section></>}
