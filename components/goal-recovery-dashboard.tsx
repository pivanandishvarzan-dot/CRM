'use client';import{useEffect,useState}from'react';import Link from'next/link';import{Badge}from'./ui';const n=(x:number)=>Number(x||0).toLocaleString('fa-IR',{maximumFractionDigits:1}),m=(x:number)=>new Intl.NumberFormat('fa-IR',{maximumFractionDigits:0}).format(Number(x||0));export default function GoalRecoveryDashboard(){const[d,setD]=useState<any>(null),[e,setE]=useState('');useEffect(()=>{fetch('/api/management/goal-recovery').then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error);setD(j.data)}).catch(x=>setE(x.message))},[]);if(e)return <div className="card p-6 text-red-700">{e}</div>;if(!d)return <div className="card p-6">در حال ساخت Action Plan...</div>;return <><div className="mb-6"><p className="text-sm text-slate-500">{new Intl.DateTimeFormat('fa-IR-u-ca-persian',{weekday:'long',day:'numeric',month:'long',year:'numeric'}).format(new Date())}</p><h1 className="mt-1 text-2xl font-bold">Goal Recovery Action Plan</h1><p className="mt-2 text-sm text-slate-500">برای مشاورهای عقب از هدف، Leadها و اقدام‌های با اولویت بالاتر مشخص می‌شوند.</p></div><div className="grid gap-3 sm:grid-cols-3"><div className="card p-4"><small>Agent نیازمند Recovery</small><b className="block text-2xl">{n(d.summary.atRiskAgents)}</b></div><div className="card p-4"><small>Lead اولویت‌دار</small><b className="block text-2xl">{n(d.summary.priorityLeads)}</b></div><div className="card p-4"><small>Follow-up عقب‌افتاده</small><b className="block text-2xl">{n(d.summary.overdueFollowups)}</b></div></div><div className="mt-5 space-y-5">{d.agents.map((a:any)=><section key={a.userId} className="card p-5"><div className="flex flex-wrap justify-between gap-4"><div><Badge tone={a.attainment>=75?'amber':'red'}>{n(a.attainment)}٪ هدف</Badge><h2 className="mt-2 text-xl font-bold">{a.name}</h2><p className="mt-1 text-sm text-slate-500">Gap: {m(a.gap)} · حدود {n(a.dealsNeeded)} قرارداد / {n(a.leadsNeeded)} Lead لازم</p></div><div className="text-left"><small>Forecast</small><b className="block text-xl">{m(a.forecast)}</b></div></div><div className="mt-4 grid gap-2 md:grid-cols-3">{a.weeklyActions.map((x:string,i:number)=><div key={i} className="rounded-xl border p-3 text-sm">{x}</div>)}</div><h3 className="mt-5 font-bold">Leadهای پیشنهادی برای تمرکز</h3><div className="mt-3 grid gap-3 md:grid-cols-2">{a.priorityLeads.map((x:any)=><Link href={`/applicants/${x.id}`} key={x.id} className="rounded-xl border p-4"><div className="flex justify-between gap-3"><div><b>{x.name}</b><p className="mt-1 text-xs text-slate-500">{x.status} · فوریت {n(x.urgency)} {x.value?`· بودجه ${m(x.value)}`:''}</p></div><Badge tone={x.score>=80?'green':x.score>=60?'amber':'red'}>{n(x.score)}</Badge></div><p className="mt-3 text-sm">{x.recommendedAction}</p>{x.hasOverdue&&<p className="mt-2 text-xs text-red-700">Follow-up عقب‌افتاده دارد</p>}</Link>)}</div></section>)}{!d.agents.length&&<div className="card p-8 text-center text-emerald-700">بر اساس Forecast فعلی، Agent دارای Gap ثبت‌شده وجود ندارد.</div>}</div></>}
