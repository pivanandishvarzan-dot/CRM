'use client';import{useEffect,useState}from'react';import{Badge}from'./ui';const n=(x:number)=>Number(x||0).toLocaleString('fa-IR');export default function TeamCapacityDashboard(){const[d,setD]=useState<any>(null),[e,setE]=useState('');useEffect(()=>{fetch('/api/management/team-capacity').then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error);setD(j.data)}).catch(x=>setE(x.message))},[]);if(e)return <div className="card p-6 text-red-700">{e}</div>;if(!d)return <div className="card p-6">در حال محاسبه ظرفیت تیم...</div>;return <><div className="mb-6"><p className="text-sm text-slate-500">{new Intl.DateTimeFormat('fa-IR-u-ca-persian',{weekday:'long',day:'numeric',month:'long',year:'numeric'}).format(new Date())}</p><h1 className="mt-1 text-2xl font-bold">Team Capacity & Load Balancing</h1><p className="mt-2 text-sm text-slate-500">توزیع Leadها بر اساس بار کاری، Leadهای داغ و وضعیت Coaching هر مشاور.</p></div><div className="grid gap-3 sm:grid-cols-5">{[['Agent',d.summary.agents],['Overloaded',d.summary.overloaded],['Available',d.summary.available],['Unassigned',d.summary.unassigned],['Open Leads',d.summary.totalOpen]].map(([a,b]:any)=><div key={a} className="card p-4"><small>{a}</small><b className="block text-2xl">{n(b)}</b></div>)}</div><section className="card mt-5 overflow-x-auto p-5"><h2 className="font-bold">Capacity by Agent</h2><table className="mt-4 w-full min-w-[800px] text-sm"><thead><tr><th className="text-right">مشاور</th><th>Load</th><th>Hot</th><th>Capacity</th><th>Utilization</th><th>وضعیت</th><th>Coaching</th></tr></thead><tbody>{d.agents.map((x:any)=><tr key={x.userId} className="border-t"><td className="p-3 font-medium">{x.name}</td><td>{n(x.load)}</td><td>{n(x.hot)}</td><td>{n(x.capacity)}</td><td>{n(x.utilization)}٪</td><td><Badge tone={x.status==='OVERLOADED'?'red':x.status==='AVAILABLE'?'green':'amber'}>{x.status}</Badge></td><td>{x.coachingStatus}</td></tr>)}</tbody></table></section><section className="card mt-5 p-5"><h2 className="font-bold">Rebalancing Suggestions</h2><div className="mt-3 space-y-3">{d.recommendations.map((x:any,i:number)=><div key={i} className="rounded-xl border p-4"><b>{x.from} → {x.to}</b><p className="mt-1 text-sm">انتقال پیشنهادی: {n(x.count)} Lead</p><p className="mt-1 text-xs text-slate-500">{x.reason}</p></div>)}{!d.recommendations.length&&<p className="text-sm text-emerald-700">عدم تعادل شدید در بار کاری شناسایی نشده است.</p>}</div></section><p className="mt-4 text-xs text-slate-400">این صفحه فقط پیشنهاد Rebalancing می‌دهد و مالکیت Lead بدون تأیید مدیر تغییر نمی‌کند.</p></>}
