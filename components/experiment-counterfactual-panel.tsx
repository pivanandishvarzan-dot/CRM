'use client';import{useEffect,useState}from'react';import{Badge}from'./ui';export default function ExperimentCounterfactualPanel(){const[items,setItems]=useState<any[]>([]);useEffect(()=>{fetch('/api/management/lead-assignment/experiment/decision').then(r=>r.ok?r.json():null).then(j=>setItems(j?.data?.counterfactuals||[])).catch(()=>{})},[]);if(!items.length)return null;return <section className="card mb-5 p-6"><div><h2 className="font-bold">چه چیزی این تصمیم را تغییر می‌دهد؟</h2><p className="mt-1 text-sm text-slate-500">شرایطی که با تغییر آن‌ها موتور تصمیم دوباره ارزیابی می‌شود؛ این‌ها پیش‌بینی قطعی نتیجه نیستند.</p></div><div className="mt-4 grid gap-3 md:grid-cols-2">{items.map((x:any)=>{const tone=x.priority==='HIGH'?'red':x.priority==='MEDIUM'?'amber':'green';return <div key={x.code} className="rounded-xl border border-slate-200 p-4"><div className="flex flex-wrap items-center justify-between gap-2"><b>{x.title}</b><Badge tone={tone}>{x.priority==='HIGH'?'اولویت بالا':x.priority==='MEDIUM'?'اولویت متوسط':'اطلاعات'}</Badge></div><div className="mt-3 grid grid-cols-2 gap-2 text-sm"><div className="rounded-lg bg-slate-50 p-3"><small className="block text-slate-400">وضعیت فعلی</small><b className="mt-1 block">{x.current}</b></div><div className="rounded-lg bg-slate-50 p-3"><small className="block text-slate-400">شرط هدف</small><b className="mt-1 block">{x.target}</b></div></div><p className="mt-3 text-sm leading-6 text-slate-600">{x.detail}</p></div>})}</div></section>}
