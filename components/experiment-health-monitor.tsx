'use client';import{useEffect,useState}from'react';import{Badge}from'./ui';const pct=(x:number)=>`${Number(x||0).toLocaleString('fa-IR')}٪`;export default function ExperimentHealthMonitor(){const[d,setD]=useState<any>(null),[e,setE]=useState('');useEffect(()=>{fetch('/api/management/lead-assignment/experiment/health').then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error);setD(j.data)}).catch(x=>setE(x.message))},[]);if(e)return <div className="card p-6 text-red-700">{e}</div>;if(!d)return <div className="card p-6">در حال بررسی سلامت آزمایش...</div>;if(!d.experiment)return <div className="card p-8 text-center">آزمایش فعالی وجود ندارد.</div>;return <><div className="mb-6"><p className="text-sm text-slate-500">{new Intl.DateTimeFormat('fa-IR-u-ca-persian',{weekday:'long',day:'numeric',month:'long',year:'numeric'}).format(new Date())}</p><h1 className="mt-1 text-2xl font-bold">Experiment Health Monitor</h1><p className="mt-2 text-sm text-slate-500">کنترل کیفیت داده و اعتبار آزمایش قبل از تصمیم‌گیری.</p></div><div className="grid gap-3 md:grid-cols-5"><div className="card p-4"><small>Health Score</small><b className="block text-2xl">{d.score.toLocaleString('fa-IR')}</b><Badge tone={d.status==='HEALTHY'?'green':d.status==='WARNING'?'amber':'red'}>{d.status}</Badge></div><div className="card p-4"><small>نمونه کل</small><b className="block text-2xl">{d.metrics.total.toLocaleString('fa-IR')}</b></div><div className="card p-4"><small>Smart واقعی / هدف</small><b className="block text-xl">{pct(d.metrics.smartShare)} / {pct(d.metrics.targetSmartShare)}</b></div><div className="card p-4"><small>ورودی ۷ روز</small><b className="block text-2xl">{d.metrics.last7Days.toLocaleString('fa-IR')}</b></div><div className="card p-4"><small>سن آزمایش</small><b className="block text-2xl">{d.metrics.ageDays.toLocaleString('fa-IR')} روز</b></div></div><section className="card mt-5 p-5"><h2 className="font-bold">Quality Guardrails</h2>{d.issues.length?<div className="mt-4 space-y-3">{d.issues.map((x:any)=><div key={x.code} className="rounded-xl border p-4"><div className="flex items-center gap-2"><Badge tone={x.severity==='HIGH'?'red':'amber'}>{x.severity==='HIGH'?'مهم':'هشدار'}</Badge><b>{x.title}</b></div><p className="mt-2 text-sm text-slate-600">{x.detail}</p></div>)}</div>:<p className="mt-4 text-sm text-emerald-700">در حال حاضر مشکل مهمی در کیفیت آزمایش شناسایی نشده است.</p>}</section><div className="mt-5 grid gap-3 sm:grid-cols-4"><div className="card p-4"><small>Sample imbalance</small><b className="block text-xl">{pct(d.metrics.imbalance)}</b></div><div className="card p-4"><small>بدون Assignment</small><b className="block text-xl">{d.metrics.unassigned.toLocaleString('fa-IR')}</b></div><div className="card p-4"><small>Outcome معطل</small><b className="block text-xl">{d.metrics.stale.toLocaleString('fa-IR')}</b></div><div className="card p-4"><small>ارزیابی Smart / Control</small><b className="block text-xl">{d.metrics.smartEvaluated.toLocaleString('fa-IR')} / {d.metrics.controlEvaluated.toLocaleString('fa-IR')}</b></div></div></>}
