'use client';import{useEffect,useState}from'react';import{Badge}from'./ui';const pct=(x:number)=>`${Number(x||0).toLocaleString('fa-IR')}٪`;export default function WorkloadBalancingDashboard(){const[d,setD]=useState<any>(null),[e,setE]=useState('');useEffect(()=>{fetch('/api/management/workload').then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error);setD(j.data)}).catch(x=>setE(x.message))},[]);if(e)return <div className="card p-6 text-red-700">{e}</div>;if(!d)return <div className="card p-6">در حال محاسبه ظرفیت تیم...</div>;return <><div className="mb-6"><p className="text-sm text-slate-500">{new Intl.DateTimeFormat('fa-IR-u-ca-persian',{weekday:'long',day:'numeric',month:'long',year:'numeric'}).format(new Date())}</p><h1 className="mt-1 text-2xl font-bold">Workload Balancing</h1><p className="mt-2 text-sm text-slate-500">پیشنهاد تخصیص Lead بر اساس بار واقعی، پیگیری‌های عقب‌افتاده و رفتار Focus Mode.</p></div>{d.recommended&&<div className="mb-5 rounded-2xl border border-emerald-200 bg-emerald-50 p-5"><small className="text-emerald-700">بهترین ظرفیت فعلی برای Lead بعدی</small><h2 className="mt-1 text-xl font-bold">{d.recommended.name}</h2><p className="mt-2 text-sm text-emerald-800">Capacity Score: {d.recommended.capacityScore.toLocaleString('fa-IR')} از ۱۰۰</p></div>}<div className="card overflow-x-auto p-5"><table className="w-full min-w-[850px] text-sm"><thead><tr className="text-right text-xs text-slate-500"><th className="p-2">مشاور</th><th>Load</th><th>Capacity</th><th>Lead فعال</th><th>Follow-up باز</th><th>عقب‌افتاده</th><th>Completion</th><th>Snooze</th><th>Pipeline</th></tr></thead><tbody>{d.agents.map((x:any)=><tr className="border-t" key={x.id}><td className="p-3 font-medium">{x.name}</td><td><Badge tone={x.loadScore>=75?'red':x.loadScore>=50?'amber':'green'}>{x.loadScore.toLocaleString('fa-IR')}</Badge></td><td>{x.capacityScore.toLocaleString('fa-IR')}</td><td>{x.activeLeads.toLocaleString('fa-IR')}</td><td>{x.openFollowups.toLocaleString('fa-IR')}</td><td>{x.overdue.toLocaleString('fa-IR')}</td><td>{pct(x.completionRate)}</td><td>{pct(x.snoozeRate)}</td><td>{Number(x.pipeline).toLocaleString('fa-IR')}</td></tr>)}</tbody></table></div></>}
