'use client';import{useEffect,useState}from'react';import{Badge}from'./ui';const pct=(x:number)=>`${Number(x||0).toLocaleString('fa-IR')}٪`,tm=(x:number)=>x>=60?`${(x/60).toLocaleString('fa-IR',{maximumFractionDigits:1})} ساعت`:`${Number(x||0).toLocaleString('fa-IR')} دقیقه`;export default function GovernanceSlaDashboard(){const[d,setD]=useState<any>(null),[e,setE]=useState('');useEffect(()=>{fetch('/api/management/ai-governance/sla').then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error);setD(j.data)}).catch(x=>setE(x.message))},[]);if(e)return <div className="card p-6 text-red-700">{e}</div>;if(!d)return <div className="card p-6">در حال محاسبه SLA...</div>;const s=d.summary;return <><div className="mb-6"><p className="text-sm text-slate-500">{new Intl.DateTimeFormat('fa-IR-u-ca-persian',{weekday:'long',day:'numeric',month:'long',year:'numeric'}).format(new Date())}</p><h1 className="mt-1 text-2xl font-bold">Governance SLA & Accountability</h1><p className="mt-2 text-sm text-slate-500">سرعت پذیرش، Resolution و رعایت SLA در رسیدگی به ریسک‌های AI.</p></div><div className="grid gap-3 sm:grid-cols-3 lg:grid-cols-6">{[['Escalation',s.total],['Ack Rate',pct(s.ackRate)],['SLA Met',pct(s.slaRate)],['Resolution',pct(s.resolutionRate)],['Avg Ack',tm(s.avgAckMinutes)],['Overdue',s.overdueOpen]].map(([a,b]:any)=><div key={a} className="card p-4"><small>{a}</small><b className="block text-xl">{typeof b==='number'?b.toLocaleString('fa-IR'):b}</b></div>)}</div><section className="card mt-5 overflow-x-auto p-5"><h2 className="font-bold">Accountability by Owner</h2><table className="mt-4 w-full min-w-[700px] text-sm"><thead><tr><th className="text-right">مسئول</th><th>موارد</th><th>SLA</th><th>Resolution</th><th>Avg Ack</th><th>Avg Resolve</th></tr></thead><tbody>{d.owners.map((x:any)=><tr key={x.ownerId} className="border-t"><td className="p-3 font-medium">{x.name}</td><td>{x.total.toLocaleString('fa-IR')}</td><td><Badge tone={x.slaRate>=90?'green':x.slaRate>=70?'amber':'red'}>{pct(x.slaRate)}</Badge></td><td>{pct(x.resolutionRate)}</td><td>{tm(x.avgAckMinutes)}</td><td>{tm(x.avgResolveMinutes)}</td></tr>)}</tbody></table>{!d.owners.length&&<p className="mt-4 text-sm text-slate-500">هنوز Escalation دارای Owner ثبت نشده است.</p>}</section><section className="card mt-5 p-5"><h2 className="font-bold">Bottleneck Signal</h2><p className="mt-3 text-sm text-slate-600">{s.overdueOpen>0?`${s.overdueOpen.toLocaleString('fa-IR')} مورد از SLA عبور کرده و نیازمند تعیین تکلیف مدیریتی است.`:s.slaRate>=90?'فرآیند پاسخ Governance در محدوده مناسب SLA قرار دارد.':'SLA هنوز پایدار نیست؛ Ownerهای با پایین‌ترین نرخ رعایت SLA در جدول بالا اولویت بررسی هستند.'}</p></section></>}
