'use client';import{useEffect,useState}from'react';import{Badge}from'./ui';const pct=(x:number)=>`${Number(x||0).toLocaleString('fa-IR')}٪`;const fa=(x:string)=>new Intl.DateTimeFormat('fa-IR-u-ca-persian',{day:'numeric',month:'long',year:'numeric'}).format(new Date(x));export default function InterventionImpactDashboard(){const[d,setD]=useState<any>(null),[error,setError]=useState('');useEffect(()=>{fetch('/api/management/interventions/impact').then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error);setD(j.data)}).catch(e=>setError(e.message))},[]);if(error)return <div className="card p-6 text-red-700">{error}</div>;if(!d)return <div className="card p-6 text-slate-500">در حال تحلیل اثر مداخلات...</div>;return <><div className="mb-6"><p className="text-sm text-slate-500">{new Intl.DateTimeFormat('fa-IR-u-ca-persian',{weekday:'long',day:'numeric',month:'long',year:'numeric'}).format(new Date())}</p><h1 className="mt-1 text-2xl font-bold">تحلیل اثر مداخلات مدیریتی</h1><p className="mt-2 text-sm text-slate-500">اندازه‌گیری زمان حل و حرکت واقعی پرونده‌ها بعد از ورود مدیر.</p></div><div className="grid gap-3 md:grid-cols-6"><div className="card p-4"><small>کل مداخلات</small><b className="mt-2 block text-2xl">{d.summary.total.toLocaleString('fa-IR')}</b></div><div className="card p-4"><small>حل‌شده</small><b className="mt-2 block text-2xl">{pct(d.summary.resolutionRate)}</b></div><div className="card p-4"><small>میانگین زمان حل</small><b className="mt-2 block text-2xl">{d.summary.avgResolutionHours.toLocaleString('fa-IR')} ساعت</b></div><div className="card p-4"><small>اثر مثبت</small><b className="mt-2 block text-2xl">{pct(d.summary.impactRate)}</b></div><div className="card p-4"><small>قرارداد بعد از مداخله</small><b className="mt-2 block text-2xl">{d.summary.contractsAfter.toLocaleString('fa-IR')}</b></div><div className="card p-4"><small>Stage پیشرفته</small><b className="mt-2 block text-2xl">{d.summary.advancedAfter.toLocaleString('fa-IR')}</b></div></div><div className="mt-5 grid gap-5 xl:grid-cols-2"><section className="card p-5"><h2 className="font-bold">اثربخشی بر اساس نوع مداخله</h2><div className="mt-4 space-y-3">{d.types.map((x:any)=><div key={x.key} className="rounded-xl border p-3"><div className="flex justify-between"><b className="text-sm">{x.key}</b><Badge tone={x.impactRate>=50?'green':x.impactRate>=25?'amber':'gray'}>{pct(x.impactRate)}</Badge></div><p className="mt-2 text-xs text-slate-500">{x.total.toLocaleString('fa-IR')} مورد · {x.contracts.toLocaleString('fa-IR')} قرارداد · میانگین حل {x.avgResolutionHours.toLocaleString('fa-IR')} ساعت</p></div>)}</div></section><section className="card p-5"><h2 className="font-bold">اثربخشی بر اساس مدیر</h2><div className="mt-4 space-y-3">{d.managers.map((x:any)=><div key={x.key} className="rounded-xl border p-3"><div className="flex justify-between"><b className="text-sm">{x.key}</b><Badge tone={x.impactRate>=50?'green':x.impactRate>=25?'amber':'gray'}>{pct(x.impactRate)}</Badge></div><p className="mt-2 text-xs text-slate-500">{x.total.toLocaleString('fa-IR')} مداخله · نرخ حل {pct(x.resolutionRate)} · {x.contracts.toLocaleString('fa-IR')} قرارداد</p></div>)}</div></section></div><section className="card mt-5 overflow-x-auto p-5"><h2 className="font-bold">مداخلات اخیر</h2><table className="mt-4 w-full min-w-[800px] text-sm"><thead><tr className="text-right text-xs text-slate-500"><th className="p-2">مورد</th><th>مدیر</th><th>وضعیت</th><th>تاریخ شروع</th><th>اثر بعدی</th><th>نتیجه</th></tr></thead><tbody>{d.recent.map((x:any)=><tr key={x.id} className="border-t"><td className="p-3 font-medium">{x.title}</td><td>{x.manager||'—'}</td><td><Badge tone={x.status==='RESOLVED'?'green':x.status==='ESCALATED'?'red':'amber'}>{x.status}</Badge></td><td>{fa(x.createdAt)}</td><td>{x.contractAfter?'قرارداد':x.advancedAfter?'پیشرفت Stage':'بدون اثر ثبت‌شده'}</td><td>{x.resolution||'—'}</td></tr>)}</tbody></table></section></>}
