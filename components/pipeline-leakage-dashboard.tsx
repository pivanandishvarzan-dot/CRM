'use client';import{useEffect,useState}from'react';import{Badge}from'./ui';const n=(x:number)=>Number(x||0).toLocaleString('fa-IR',{maximumFractionDigits:1}),labels:any={PRICE_BUDGET:'قیمت / بودجه',COMPETITOR:'رقیب',FOLLOWUP:'ضعف پیگیری',NO_MATCH:'عدم تطابق ملک',CUSTOMER_CHANGED:'تغییر تصمیم مشتری',UNKNOWN:'نامشخص'};export default function PipelineLeakageDashboard(){const[d,setD]=useState<any>(null),[e,setE]=useState('');useEffect(()=>{fetch('/api/management/pipeline-leakage').then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error);setD(j.data)}).catch(x=>setE(x.message))},[]);if(e)return <div className="card p-6 text-red-700">{e}</div>;if(!d)return <div className="card p-6">در حال تحلیل Leakage...</div>;return <><div className="mb-6"><p className="text-sm text-slate-500">۹۰ روز اخیر</p><h1 className="mt-1 text-2xl font-bold">Pipeline Leakage Analytics</h1><p className="mt-2 text-sm text-slate-500">بررسی فرصت‌های Lost برای فهمیدن اینکه فروش دقیقاً کجا و چرا از Pipeline خارج می‌شود.</p></div><div className="grid gap-3 sm:grid-cols-5">{[['Closed',d.summary.closed],['Won',d.summary.won],['Lost',d.summary.lost],['Leak Rate',`${n(d.summary.leakRate)}٪`],['دلیل نامشخص',d.summary.unknownReasons]].map(([a,b]:any)=><div key={a} className="card p-4"><small>{a}</small><b className="block text-2xl">{typeof b==='number'?n(b):b}</b></div>)}</div><div className="mt-5 grid gap-4 lg:grid-cols-2"><section className="card p-5"><h2 className="font-bold">Lost Reasons</h2><div className="mt-4 space-y-3">{d.reasons.map((x:any)=><div key={x.reason} className="flex justify-between rounded-xl border p-4"><span>{labels[x.reason]||x.reason}</span><b>{n(x.count)}</b></div>)}{!d.reasons.length&&<p className="text-sm text-slate-500">Lost ثبت‌شده‌ای در بازه وجود ندارد.</p>}</div></section><section className="card overflow-x-auto p-5"><h2 className="font-bold">Leakage by Agent</h2><table className="mt-4 w-full min-w-[500px] text-sm"><thead><tr><th className="text-right">Agent</th><th>Won</th><th>Lost</th><th>Leak Rate</th></tr></thead><tbody>{d.agents.map((x:any)=><tr key={x.agentId||x.name} className="border-t"><td className="p-3 font-medium">{x.name}</td><td>{n(x.won)}</td><td>{n(x.lost)}</td><td><Badge tone={x.leakRate<=30?'green':x.leakRate<=50?'amber':'red'}>{n(x.leakRate)}٪</Badge></td></tr>)}</tbody></table></section></div><section className="card mt-5 p-5"><h2 className="font-bold">Data Quality Signal</h2><p className="mt-2 text-sm text-slate-600">{d.summary.unknownReasons>0?`${n(d.summary.unknownReasons)} مورد Lost دلیل قابل تشخیص ندارند. ثبت ساختاریافته Lost Reason باید در مرحله بعد اجباری شود.`:'دلایل Lost فعلی قابل دسته‌بندی هستند.'}</p></section></>}
