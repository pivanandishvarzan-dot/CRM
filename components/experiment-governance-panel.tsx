'use client';import{useEffect,useState}from'react';import{Badge}from'./ui';export default function ExperimentGovernancePanel(){const[g,setG]=useState<any>(null);useEffect(()=>{fetch('/api/management/lead-assignment/experiment/decision').then(r=>r.ok?r.json():null).then(j=>setG(j?.data?.governance||null)).catch(()=>{})},[]);if(!g)return null;const tone=g.level==='STRONG'?'green':g.level==='WEAK'?'red':'amber',score=Math.max(0,Math.min(100,Number(g.score||0)));return <section className="card mb-5 p-6"><div className="flex flex-wrap items-start justify-between gap-4"><div><div className="flex flex-wrap items-center gap-2"><h2 className="font-bold">Decision Governance</h2><Badge tone={tone}>{g.level}</Badge></div><p className="mt-2 text-sm text-slate-600">{g.title} — {g.detail}</p></div><div className="text-left"><b className="text-3xl">{score.toLocaleString('fa-IR')}</b><span className="text-sm text-slate-400"> / ۱۰۰</span></div></div><div className="mt-5 h-3 overflow-hidden rounded-full bg-slate-100"><div className={`h-full rounded-full ${g.level==='STRONG'?'bg-emerald-500':g.level==='WEAK'?'bg-red-500':'bg-amber-500'}`} style={{width:`${score}%`}}/></div><div className="mt-5 grid gap-3 md:grid-cols-2 xl:grid-cols-4">{(g.checks||[]).map((x:any)=><div key={x.code} className={`rounded-xl border p-4 ${x.passed?'border-emerald-200 bg-emerald-50':'border-slate-200 bg-slate-50'}`}><div className="flex items-center justify-between gap-2"><b className="text-sm">{x.label}</b><Badge tone={x.passed?'green':'amber'}>{x.passed?'PASS':'REVIEW'}</Badge></div><p className="mt-2 text-xs leading-5 text-slate-500">{x.detail}</p></div>)}</div><p className="mt-4 text-xs text-slate-400">Governance Score کیفیت فرایند تصمیم را نشان می‌دهد و جایگزین نتیجه آماری آزمایش نیست.</p></section>}
