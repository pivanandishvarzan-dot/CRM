'use client';import{useEffect,useState}from'react';import{Badge}from'./ui';type D={summary:any;agents:any[];speedBuckets:any[]};const pct=(x:number)=>`${Number(x||0).toLocaleString('fa-IR')}٪`;export default function RecommendationAdoptionDashboard(){const[d,setD]=useState<D|null>(null),[error,setError]=useState('');useEffect(()=>{fetch('/api/recommendations/adoption').then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error);setD(j.data)}).catch(e=>setError(e.message))},[]);if(error)return <div className="card p-6 text-red-700">{error}</div>;if(!d)return <div className="card p-6 text-slate-500">در حال تحلیل رفتار تیم...</div>;return <><div className="mb-6"><p className="text-sm text-slate-500">{new Intl.DateTimeFormat('fa-IR-u-ca-persian',{weekday:'long',day:'numeric',month:'long',year:'numeric'}).format(new Date())}</p><h1 className="mt-1 text-2xl font-bold">Recommendation Adoption Score</h1><p className="mt-2 text-sm text-slate-500">سرعت استفاده از پیشنهادهای CRM و رابطه آن با نتیجه واقعی فروش.</p></div><div className="grid gap-3 md:grid-cols-4"><div className="card p-4"><small>مشاور فعال</small><b className="mt-2 block text-2xl">{d.summary.agents.toLocaleString('fa-IR')}</b></div><div className="card p-4"><small>اقدام اجراشده</small><b className="mt-2 block text-2xl">{d.summary.executions.toLocaleString('fa-IR')}</b></div><div className="card p-4"><small>موفقیت اقدام سریع</small><b className="mt-2 block text-2xl">{pct(d.summary.fastSuccessRate)}</b></div><div className="card p-4"><small>موفقیت اقدام دیرهنگام</small><b className="mt-2 block text-2xl">{pct(d.summary.slowSuccessRate)}</b></div></div><div className="mt-5 grid gap-5 xl:grid-cols-[1fr_360px]"><div className="card overflow-x-auto p-5"><h2 className="font-bold">رتبه پذیرش مشاورها</h2><table className="mt-4 w-full min-w-[700px] text-sm"><thead><tr className="text-right text-xs text-slate-500"><th className="p-2">مشاور</th><th>Adoption Score</th><th>اجرا</th><th>اجرای سریع</th><th>Success</th><th>Contract</th></tr></thead><tbody>{d.agents.map((x,i)=><tr key={x.id} className="border-t"><td className="p-3">{(i+1).toLocaleString('fa-IR')}. {x.name}</td><td><Badge tone={x.adoptionScore>=70?'green':x.adoptionScore>=45?'amber':'red'}>{x.adoptionScore.toLocaleString('fa-IR')}</Badge></td><td>{x.executions.toLocaleString('fa-IR')}</td><td>{pct(x.fastShare)}</td><td>{pct(x.successRate)}</td><td>{x.contracts.toLocaleString('fa-IR')}</td></tr>)}</tbody></table></div><div className="card p-5"><h2 className="font-bold">سرعت اقدام و Conversion</h2><div className="mt-4 space-y-4">{d.speedBuckets.map(x=><div key={x.key}><div className="flex justify-between text-sm"><span>{x.label}</span><b>{pct(x.successRate)}</b></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full bg-brand-500" style={{width:`${Math.min(100,x.successRate)}%`}}/></div><p className="mt-1 text-xs text-slate-400">{x.evaluated.toLocaleString('fa-IR')} نتیجه ارزیابی‌شده</p></div>)}</div></div></div></>}
