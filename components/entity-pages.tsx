'use client';
import {useEffect,useMemo,useState} from 'react';import {followups,contracts,agents} from '@/lib/demo-data';import {Header} from './properties-view';import {Badge,Modal} from './ui';import {Search,Phone,MessageSquare,Calendar,CheckCircle2,Clock,TrendingUp,Award,Trash2} from 'lucide-react';
type Owner={id:string;name:string;phone:string;email?:string;note:string;agent:string;propertyCount:number};type Applicant={id:string;name:string;phone:string;request:string;budget:string;urgency:string;agent:string;notes:string};
function Toolbar({q,setQ}:{q?:string;setQ?:(v:string)=>void}){return <div className="card mb-5 flex gap-3 p-4"><div className="relative flex-1"><Search className="absolute right-3 top-3 text-slate-400" size={18}/><input value={q??''} onChange={e=>setQ?.(e.target.value)} className="input pr-10" placeholder="جست‌وجو در فهرست..."/></div><select className="input w-40"><option>همه وضعیت‌ها</option><option>فعال</option><option>در انتظار</option></select></div>}
function Notice({loading,error}:{loading:boolean;error:string}){if(loading)return <div className="card p-8 text-center text-slate-500">در حال دریافت اطلاعات...</div>;if(error)return <div className="card p-5 text-red-600">{error}</div>;return null}
export function Owners(){const[open,setOpen]=useState(false);const[data,setData]=useState<Owner[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState('');const[q,setQ]=useState('');async function load(){setLoading(true);try{const r=await fetch('/api/owners',{cache:'no-store'});const j=await r.json();if(!r.ok)throw new Error(j.error);setData(j.data)}catch(e){setError(e instanceof Error?e.message:'خطا در دریافت مالک‌ها')}finally{setLoading(false)}}useEffect(()=>{void load()},[]);const shown=useMemo(()=>data.filter(o=>(o.name+o.phone+o.agent).includes(q)),[data,q]);async function add(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch('/api/owners',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:f.get('name'),phone:f.get('phone'),email:f.get('email'),notes:f.get('notes')})});const j=await r.json();if(!r.ok){setError(j.error);return}setData(x=>[j.data,...x]);setOpen(false)}async function del(id:string){if(!confirm('مالک حذف شود؟'))return;const r=await fetch(`/api/owners/${id}`,{method:'DELETE'});if(!r.ok){const j=await r.json();setError(j.error);return}setData(x=>x.filter(o=>o.id!==id))}return <><Header title="مالک‌ها" sub={`${data.length} مالک ثبت‌شده · مدیریت اطلاعات، املاک و ارتباطات`} action={()=>setOpen(true)} label="مالک جدید"/><Toolbar q={q} setQ={setQ}/><Notice loading={loading} error={error}/>{!loading&&!error&&<div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{shown.map(o=><div className="card p-5" key={o.id}><div className="flex gap-3"><div className="grid h-12 w-12 place-items-center rounded-full bg-brand-100 font-bold text-brand-700">{o.name[0]}</div><div><b>{o.name}</b><p className="mt-1 text-xs text-slate-500">مشاور: {o.agent}</p></div><Badge tone="green">فعال</Badge><button onClick={()=>del(o.id)} className="mr-auto text-red-500" aria-label="حذف مالک"><Trash2 size={17}/></button></div><div className="my-4 rounded-xl bg-slate-50 p-3 text-sm"><Phone className="ml-2 inline" size={15}/>{o.phone}</div><p className="text-xs text-slate-500">{o.note||'بدون یادداشت'}</p><div className="mt-4 border-t pt-4 text-xs">{o.propertyCount} ملک ثبت‌شده</div></div>)}</div>}<OwnerForm open={open} close={()=>setOpen(false)} submit={add}/></>}
export function Applicants(){const[open,setOpen]=useState(false);const[data,setData]=useState<Applicant[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState('');const[q,setQ]=useState('');async function load(){try{const r=await fetch('/api/applicants',{cache:'no-store'});const j=await r.json();if(!r.ok)throw new Error(j.error);setData(j.data)}catch(e){setError(e instanceof Error?e.message:'خطا در دریافت متقاضی‌ها')}finally{setLoading(false)}}useEffect(()=>{void load()},[]);const shown=useMemo(()=>data.filter(a=>(a.name+a.phone+a.request+a.agent).includes(q)),[data,q]);async function add(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch('/api/applicants',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:f.get('name'),phone:f.get('phone'),requestType:f.get('requestType'),budgetMax:Number(f.get('budgetMax'))||undefined,urgency:Number(f.get('urgency'))||1,notes:f.get('notes')})});const j=await r.json();if(!r.ok){setError(j.error);return}setData(x=>[j.data,...x]);setOpen(false)}async function del(id:string){if(!confirm('متقاضی حذف شود؟'))return;const r=await fetch(`/api/applicants/${id}`,{method:'DELETE'});if(!r.ok){const j=await r.json();setError(j.error);return}setData(x=>x.filter(a=>a.id!==id))}return <><Header title="متقاضی‌ها" sub={`${data.length} متقاضی · نیازسنجی و پیشنهاد هوشمند ملک`} action={()=>setOpen(true)} label="متقاضی جدید"/><Toolbar q={q} setQ={setQ}/><Notice loading={loading} error={error}/>{!loading&&!error&&<div className="card table-wrap"><table className="data-table"><thead><tr><th>متقاضی</th><th>نوع درخواست</th><th>بودجه</th><th>فوریت</th><th>مشاور</th><th/></tr></thead><tbody>{shown.map(a=><tr key={a.id}><td><b>{a.name}</b><small className="block text-slate-500">{a.phone}</small></td><td>{a.request}</td><td>{a.budget}</td><td><Badge tone={a.urgency==='فوری'?'red':a.urgency==='زیاد'?'amber':'gray'}>{a.urgency}</Badge></td><td>{a.agent}</td><td><button onClick={()=>del(a.id)} className="text-red-500" aria-label="حذف متقاضی"><Trash2 size={17}/></button></td></tr>)}</tbody></table></div>}<ApplicantForm open={open} close={()=>setOpen(false)} submit={add}/></>}
function OwnerForm({open,close,submit}:{open:boolean;close:()=>void;submit:(e:React.FormEvent<HTMLFormElement>)=>void}){return <Modal open={open} onClose={close} title="افزودن مالک"><form onSubmit={submit} className="grid gap-4 md:grid-cols-2"><div><label className="label">نام و نام خانوادگی</label><input required name="name" className="input"/></div><div><label className="label">شماره تماس</label><input required name="phone" className="input" dir="ltr"/></div><div className="md:col-span-2"><label className="label">ایمیل</label><input name="email" type="email" className="input" dir="ltr"/></div><div className="md:col-span-2"><label className="label">یادداشت</label><textarea name="notes" className="input h-24 py-3"/></div><button className="btn-primary md:col-start-2">ذخیره مالک</button></form></Modal>}
function ApplicantForm({open,close,submit}:{open:boolean;close:()=>void;submit:(e:React.FormEvent<HTMLFormElement>)=>void}){return <Modal open={open} onClose={close} title="ثبت متقاضی جدید"><form onSubmit={submit} className="grid gap-4 md:grid-cols-2"><div><label className="label">نام و نام خانوادگی</label><input required name="name" className="input"/></div><div><label className="label">شماره تماس</label><input required name="phone" className="input" dir="ltr"/></div><div><label className="label">نوع درخواست</label><select name="requestType" className="input"><option value="SALE">خرید</option><option value="RENT">اجاره</option><option value="MORTGAGE_RENT">رهن و اجاره</option></select></div><div><label className="label">بودجه حداکثر (میلیارد)</label><input name="budgetMax" type="number" step="0.1" className="input"/></div><div><label className="label">فوریت</label><select name="urgency" className="input"><option value="1">عادی</option><option value="2">زیاد</option><option value="3">فوری</option></select></div><div className="md:col-span-2"><label className="label">یادداشت</label><textarea name="notes" className="input h-24 py-3"/></div><button className="btn-primary md:col-start-2">ذخیره متقاضی</button></form></Modal>}
function SimpleForm({open,close,title}:{open:boolean;close:()=>void;title:string}){return <Modal open={open} onClose={close} title={title}><form onSubmit={e=>{e.preventDefault();close()}} className="grid gap-4"><label className="label">عنوان / توضیحات</label><input required className="input"/><button className="btn-primary">ذخیره</button></form></Modal>}
export function Followups(){const[tab,setTab]=useState('امروز');const[open,setOpen]=useState(false);return <><Header title="پیگیری‌ها و وظایف" sub="برنامه روزانه تیم و یادآوری ارتباطات" action={()=>setOpen(true)} label="پیگیری جدید"/><div className="mb-5 flex gap-2 overflow-auto">{['امروز','عقب‌افتاده','آینده','همه'].map(x=><button onClick={()=>setTab(x)} className={tab===x?'btn-primary':'btn-secondary'} key={x}>{x}</button>)}</div><div className="grid gap-4 lg:grid-cols-3"><div className="space-y-3 lg:col-span-2">{followups.map(f=><div className="card flex items-center gap-4 p-4" key={f.id}><button className="grid h-10 w-10 place-items-center rounded-full border-2 border-slate-200 text-slate-300"><CheckCircle2 size={20}/></button><div><b>{f.title}</b><p className="mt-1 text-xs text-slate-500"><Clock className="ml-1 inline" size={13}/>{f.time} · {f.agent}</p></div><button className="btn-secondary mr-auto hidden sm:flex"><MessageSquare size={16}/>یادداشت</button></div>)}</div><div className="card p-5"><h2 className="font-bold">خلاصه امروز</h2><div className="mt-6 rounded-xl bg-brand-50 p-4 text-sm text-brand-900"><Calendar className="mb-2"/><b>بازدید بعدی</b><p className="mt-1 text-xs">ویلای لواسان، ساعت ۱۶:۰۰</p></div></div></div><SimpleForm open={open} close={()=>setOpen(false)} title="ایجاد پیگیری جدید"/></>}
export function Contracts(){return <><Header title="قراردادها" sub="مدیریت قراردادها، کمیسیون‌ها و پرداخت‌ها"/><Toolbar/><div className="card table-wrap"><table className="data-table"><thead><tr><th>شماره</th><th>ملک / طرف قرارداد</th><th>نوع</th><th>مبلغ</th><th>کمیسیون</th><th>تاریخ</th><th>وضعیت</th></tr></thead><tbody>{contracts.map(c=><tr key={c.id}><td>{c.id}</td><td><b>{c.property}</b><small className="block">{c.party}</small></td><td>{c.type}</td><td>{c.amount}</td><td>{c.commission}</td><td>{c.date}</td><td><Badge>{c.status}</Badge></td></tr>)}</tbody></table></div></>}
export function Reports(){return <><Header title="گزارش‌ها و عملکرد" sub="تحلیل عملکرد تیم فروش"/><div className="card p-6"><h2 className="mb-5 font-bold">رتبه‌بندی مشاوران</h2>{agents.map((a,i)=><div className="flex items-center gap-4 border-b py-4 last:border-0" key={a.name}><span>{i+1}</span>{i===0&&<Award className="text-amber-500"/>}<div><b>{a.name}</b><p className="text-xs text-slate-500">{a.deals} قرارداد موفق</p></div><div className="mr-auto"><TrendingUp size={16}/>{a.value} میلیارد</div></div>)}</div></>}
