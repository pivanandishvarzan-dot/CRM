'use client';import{useEffect,useState}from'react';import{Badge}from'./ui';type Action={actionKey:string;executions:number;evaluated:number;successes:number;contracts:number;stage_advanced:number;positive_matches:number;followups:number;successRate:number};type Agent={id:string;name:string;executions:number;evaluated:number;successes:number;contracts:number;successRate:number};type Data={summary:{executions:number;evaluated:number;successes:number;successRate:number;contracts:number};actions:Action[];agents:Agent[]};const label:Record<string,string>={OVERDUE_FOLLOWUP:'پیگیری عقب‌افتاده',RISK_RESCUE:'نجات معامله',REENGAGE:'ارتباط مجدد',SEND_MATCHES:'ارسال Match جدید',REQUALIFY:'بازبینی نیازسنجی',BOOK_VISIT:'هماهنگی بازدید',VISIT_FOLLOWUP:'پیگیری بازدید',NEGOTIATION:'پیگیری مذاکره',CLOSE_CONTRACT:'نهایی‌سازی قرارداد',QUALIFY:'تکمیل نیازسنجی',MAINTAIN:'پیگیری روتین'};export default function RecommendationAnalyticsCenter(){const[data,setData]=useState<Data|null>(null),[error,setError]=useState('');useEffect(()=>{fetch('/api/recommendations/analytics').then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error||'خطا');setData(j)}).catch(e=>setError(e.message))},[]);if(!data)return <div className="card p-10 text-center text-sm text-slate-500">{error||'در حال تحلیل پیشنهادهای سیستم...'}</div>;return <><div className="mb-6"><p className="text-sm text-slate-500">{new Intl.DateTimeFormat('fa-IR-u-ca-persian',{weekday:'long',day:'numeric',month:'long',year:'numeric'}).format(new Date())}</p><h1 className="mt-1 text-2xl font-bold">تحلیل پیشنهادهای هوشمند</h1><p className="mt-2 text-sm text-slate-500">اثر واقعی Next Best Actionها بر پیشرفت پرونده، پیگیری و قرارداد.</p></div><div className="mb-6 grid gap-4 md:grid-cols-4"><div className="card p-4"><small>اقدام‌های اجراشده</small><b className="mt-2 block text-xl">{data.summary.executions.toLocaleString('fa-IR')}</b></div><div className="card p-4"><small>ارزیابی‌شده</small><b className="mt-2 block text-xl">{data.summary.evaluated.toLocaleString('fa-IR')}</b></div><div className="card p-4"><small>نرخ موفقیت</small><b className="mt-2 block text-xl">{data.summary.successRate.toLocaleString('fa-IR')}٪</b></div><div className="card p-4"><small>قرارداد منتسب</small><b className="mt-2 block text-xl">{data.summary.contracts.toLocaleString('fa-IR')}</b></div></div><div className="card mb-6 table-wrap"><table className="data-table"><thead><tr><th>اقدام</th><th>اجرا</th><th>موفقیت</th><th>قرارداد</th><th>Stage جلو رفته</th><th>Match مثبت</th></tr></thead><tbody>{data.actions.map(x=><tr key={x.actionKey}><td><b>{label[x.actionKey]||x.actionKey}</b></td><td>{x.executions.toLocaleString('fa-IR')}</td><td><Badge tone={x.successRate>=70?'green':x.successRate>=45?'amber':'gray'}>{x.successRate.toLocaleString('fa-IR')}٪</Badge></td><td>{x.contracts.toLocaleString('fa-IR')}</td><td>{x.stage_advanced.toLocaleString('fa-IR')}</td><td>{x.positive_matches.toLocaleString('fa-IR')}</td></tr>)}</tbody></table></div><div className="card table-wrap"><div className="border-b p-4"><b>استفاده مشاورها از پیشنهادها</b></div><table className="data-table"><thead><tr><th>مشاور</th><th>اقدام اجراشده</th><th>ارزیابی‌شده</th><th>موفق</th><th>نرخ موفقیت</th><th>قرارداد</th></tr></thead><tbody>{data.agents.map(x=><tr key={x.id}><td><b>{x.name}</b></td><td>{x.executions.toLocaleString('fa-IR')}</td><td>{x.evaluated.toLocaleString('fa-IR')}</td><td>{x.successes.toLocaleString('fa-IR')}</td><td>{x.successRate.toLocaleString('fa-IR')}٪</td><td>{x.contracts.toLocaleString('fa-IR')}</td></tr>)}</tbody></table></div></>}
