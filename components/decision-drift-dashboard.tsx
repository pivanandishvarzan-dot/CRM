'use client';import{useEffect,useState}from'react';import{Badge}from'./ui';const pct=(x:number)=>`${Number(x||0).toLocaleString('fa-IR')}٪`;export default function DecisionDriftDashboard(){const[d,setD]=useState<any>(null),[e,setE]=useState('');useEffect(()=>{fetch('/api/management/decisions/drift').then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error);setD(j.data)}).catch(x=>setE(x.message))},[]);if(e)return <div className="card p-6 text-red-700">{e}</div>;if(!d)return <div className="card p-6">در حال بررسی Decision Drift...</div>;const tone=d.status==='STABLE'?'green':d.status==='DRIFT_DETECTED'?'red':'amber';return <><div className="mb-6"><p className="text-sm text-slate-500">{new Intl.DateTimeFormat('fa-IR-u-ca-persian',{weekday:'long',day:'numeric',month:'long',year:'numeric'}).format(new Date())}</p><h1 className="mt-1 text-2xl font-bold">Decision Drift Detection</h1><p className="mt-2 text-sm text-slate-500">مقایسه عملکرد ۳۰ تصمیم اخیر با Baseline تاریخی موتور تصمیم‌یار.</p></div><section className="card p-6"><div className="flex flex-wrap justify-between gap-4"><div><Badge tone={tone}>{d.status}</Badge><h2 className="mt-3 text-xl font-bold">{d.status==='STABLE'?'عملکرد تصمیم‌یار پایدار است':d.status==='DRIFT_DETECTED'?'Drift معنادار شناسایی شد':d.status==='WARNING'?'نشانه‌های اولیه Drift دیده می‌شود':'برای تشخیص Drift داده کافی نیست'}</h2></div><div><small>Drift Score</small><b className="block text-3xl">{Number(d.driftScore||0).toLocaleString('fa-IR')}</b></div></div></section><div className="mt-5 grid gap-4 md:grid-cols-2"><div className="card p-5"><h2 className="font-bold">۳۰ تصمیم اخیر</h2><div className="mt-4 grid grid-cols-3 gap-3"><div><small>نمونه</small><b className="block text-xl">{Number(d.recent?.n||0).toLocaleString('fa-IR')}</b></div><div><small>Accuracy</small><b className="block text-xl">{pct(d.recent?.accuracy)}</b></div><div><small>Calibration Error</small><b className="block text-xl">{pct(d.recent?.calibrationError)}</b></div></div></div><div className="card p-5"><h2 className="font-bold">Baseline</h2><div className="mt-4 grid grid-cols-3 gap-3"><div><small>نمونه</small><b className="block text-xl">{Number(d.baseline?.n||0).toLocaleString('fa-IR')}</b></div><div><small>Accuracy</small><b className="block text-xl">{pct(d.baseline?.accuracy)}</b></div><div><small>Calibration Error</small><b className="block text-xl">{pct(d.baseline?.calibrationError)}</b></div></div></div></div><section className="card mt-5 p-5"><h2 className="font-bold">Drift Signals</h2>{d.issues?.length?<div className="mt-4 space-y-3">{d.issues.map((x:any)=><div key={x.code} className="rounded-xl border p-4"><div className="flex gap-2"><Badge tone={x.severity==='HIGH'?'red':'amber'}>{x.severity}</Badge><b>{x.title}</b></div><p className="mt-2 text-sm text-slate-600">{x.detail}</p></div>)}</div>:<p className="mt-4 text-sm text-emerald-700">سیگنال Drift مهمی شناسایی نشده است.</p>}</section></>}
