'use client';import{useEffect,useState}from'react';import{Badge}from'./ui';const n=(x:number)=>Number(x||0).toLocaleString('fa-IR',{maximumFractionDigits:1}),money=(x:number)=>new Intl.NumberFormat('fa-IR',{maximumFractionDigits:0}).format(Number(x||0)),month=(x:string)=>new Intl.DateTimeFormat('fa-IR-u-ca-persian',{month:'short',year:'numeric'}).format(new Date(x));export default function RevenueForecastDashboard(){const[d,setD]=useState<any>(null),[e,setE]=useState('');useEffect(()=>{fetch('/api/management/revenue-forecast').then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error);setD(j.data)}).catch(x=>setE(x.message))},[]);if(e)return <div className="card p-6 text-red-700">{e}</div>;if(!d)return <div className="card p-6">در حال محاسبه Forecast...</div>;const s=d.summary,max=Math.max(...d.history.map((x:any)=>x.revenue),s.expectedRevenue,1);return <><div className="mb-6"><p className="text-sm text-slate-500">{new Intl.DateTimeFormat('fa-IR-u-ca-persian',{weekday:'long',day:'numeric',month:'long',year:'numeric'}).format(new Date())}</p><h1 className="mt-1 text-2xl font-bold">Revenue & Sales Forecast</h1><p className="mt-2 text-sm text-slate-500">پیش‌بینی احتمالی قرارداد و کمیسیون بر اساس Pipeline، Stage و عملکرد تاریخی.</p></div><section className="card p-5"><div className="flex justify-between gap-4"><div><Badge tone={s.confidence==='HIGH'?'green':s.confidence==='MEDIUM'?'amber':'red'}>Confidence {s.confidence}</Badge><h2 className="mt-3 text-xl font-bold">Forecast ماه جاری</h2></div><div className="text-left"><small>Expected Revenue</small><b className="block text-3xl">{money(s.expectedRevenue)}</b></div></div></section><div className="mt-5 grid gap-3 sm:grid-cols-3 lg:grid-cols-6">{[['Pipeline باز',n(s.openPipeline)],['قرارداد مورد انتظار',n(s.expectedDeals)],['کمیسیون مورد انتظار',money(s.expectedCommission)],['قرارداد ثبت‌شده',n(s.bookedDeals)],['فروش ثبت‌شده',money(s.bookedRevenue)],['Avg Deal',money(s.avgDealValue)]].map(([a,b])=><div key={a} className="card p-4"><small>{a}</small><b className="block text-xl">{b}</b></div>)}</div><div className="mt-5 grid gap-4 lg:grid-cols-2"><section className="card p-5"><h2 className="font-bold">Forecast by Pipeline Stage</h2><div className="mt-4 space-y-3">{d.stages.map((x:any)=><div key={x.stage} className="rounded-xl border p-3"><div className="flex justify-between"><b>{x.stage}</b><span>{n(x.expectedDeals)} قرارداد</span></div><p className="mt-1 text-sm text-slate-500">{n(x.leads)} لید · Revenue وزنی {money(x.expectedRevenue)}</p></div>)}</div></section><section className="card p-5"><h2 className="font-bold">Forecast by Agent</h2><div className="mt-4 space-y-3">{d.agents.slice(0,8).map((x:any)=><div key={x.agentId} className="rounded-xl border p-3"><div className="flex justify-between"><b>{x.name}</b><b>{money(x.expectedRevenue)}</b></div><p className="mt-1 text-xs text-slate-500">{n(x.leads)} لید · {n(x.expectedDeals)} قرارداد مورد انتظار · کمیسیون {money(x.expectedCommission)}</p></div>)}</div></section></div><section className="card mt-5 p-5"><h2 className="font-bold">Revenue Trend + Forecast</h2><div className="mt-5 flex h-52 items-end gap-3">{d.history.map((x:any)=><div key={x.month} className="flex flex-1 flex-col items-center justify-end"><span className="mb-2 text-xs">{money(x.revenue)}</span><div className="w-full rounded-t bg-slate-300" style={{height:`${Math.max(3,x.revenue/max*100)}%`}}/><small className="mt-2">{month(x.month)}</small></div>)}<div className="flex flex-1 flex-col items-center justify-end"><span className="mb-2 text-xs font-bold">{money(s.expectedRevenue)}</span><div className="w-full rounded-t bg-slate-800" style={{height:`${Math.max(3,s.expectedRevenue/max*100)}%`}}/><small className="mt-2 font-bold">Forecast</small></div></div></section><p className="mt-4 text-xs text-slate-400">Forecast تصمیم‌یار است و تعهد مالی محسوب نمی‌شود؛ با افزایش داده قراردادهای واقعی Confidence بالاتر می‌رود.</p></>}
