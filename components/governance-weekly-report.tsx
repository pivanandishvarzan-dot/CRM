'use client';import{useEffect,useState}from'react';import{Badge}from'./ui';const pct=(x:number)=>`${Number(x||0).toLocaleString('fa-IR')}٪`,date=(x:string)=>new Intl.DateTimeFormat('fa-IR-u-ca-persian',{day:'numeric',month:'long',year:'numeric'}).format(new Date(x));export default function GovernanceWeeklyReport(){const[d,setD]=useState<any>(null),[e,setE]=useState('');useEffect(()=>{fetch('/api/management/ai-governance/weekly-report').then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error);setD(j.data)}).catch(x=>setE(x.message))},[]);if(e)return <div className="card p-6 text-red-700">{e}</div>;if(!d)return <div className="card p-6">در حال ساخت گزارش هفتگی...</div>;return <><div className="mb-6"><p className="text-sm text-slate-500">{date(d.period.start)} تا {date(d.period.end)}</p><h1 className="mt-1 text-2xl font-bold">Weekly AI Governance Report</h1><p className="mt-2 text-sm text-slate-500">خلاصه اجرایی وضعیت اعتماد، ریسک و پاسخ مدیریتی در هفت روز اخیر.</p></div><section className="card p-6"><div className="flex flex-wrap justify-between gap-4"><div><Badge tone={d.posture==='STABLE'?'green':d.posture==='CRITICAL'?'red':'amber'}>{d.posture}</Badge><h2 className="mt-3 text-xl font-bold">{d.headline}</h2><p className="mt-2 text-sm text-slate-600"><b>اقدام اصلی هفته:</b> {d.executiveAction}</p></div><div><small>AI Trust</small><b className="block text-3xl">{d.trust.score.toLocaleString('fa-IR')} / ۱۰۰</b></div></div></section><div className="mt-5 grid gap-3 sm:grid-cols-4"><div className="card p-4"><small>SLA Met</small><b className="block text-2xl">{pct(d.sla.slaRate)}</b></div><div className="card p-4"><small>Incident باز</small><b className="block text-2xl">{d.counts.openIncidents.toLocaleString('fa-IR')}</b></div><div className="card p-4"><small>Drift Score</small><b className="block text-2xl">{Number(d.drift.score||0).toLocaleString('fa-IR')}</b></div><div className="card p-4"><small>Calibration Error</small><b className="block text-2xl">{pct(d.calibration.calibrationError)}</b></div></div><div className="mt-5 grid gap-4 lg:grid-cols-2"><section className="card p-5"><h2 className="font-bold">ریسک‌های اصلی</h2><div className="mt-3 space-y-2">{d.risks.map((x:string,i:number)=><div key={i} className="rounded-xl border border-red-100 p-3 text-sm text-red-800">{x}</div>)}{!d.risks.length&&<p className="text-sm text-emerald-700">ریسک بحرانی فعالی در خلاصه هفتگی دیده نمی‌شود.</p>}</div></section><section className="card p-5"><h2 className="font-bold">نقاط مثبت</h2><div className="mt-3 space-y-2">{d.wins.map((x:string,i:number)=><div key={i} className="rounded-xl border p-3 text-sm text-emerald-700">{x}</div>)}{!d.wins.length&&<p className="text-sm text-slate-500">هنوز سیگنال مثبت کافی برای برجسته‌سازی وجود ندارد.</p>}</div></section></div></>}
