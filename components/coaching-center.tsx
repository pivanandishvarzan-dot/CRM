'use client';import{useEffect,useState}from'react';import{Badge}from'./ui';type Row={id:string;name:string;month:string;overall:number;label:string;metrics:{target:number;actual:number;quotaPct:number;leads:number;wins:number;conversion:number;discipline:number;response:number;overdue:number};issues:{label:string;score:number;advice:string}[];primaryAdvice:string};const n=(v:number)=>v.toLocaleString('fa-IR',{maximumFractionDigits:1});export default function CoachingCenter(){const[rows,setRows]=useState<Row[]>([]),[error,setError]=useState('');useEffect(()=>{fetch('/api/coaching').then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error||'خطا');setRows(j.data||[])}).catch(e=>setError(e.message))},[]);return <><div className="mb-6"><p className="text-sm text-slate-500">{new Intl.DateTimeFormat('fa-IR-u-ca-persian',{weekday:'long',day:'numeric',month:'long',year:'numeric'}).format(new Date())}</p><h1 className="mt-1 text-2xl font-bold">مربی عملکرد مشاوران</h1><p className="mt-2 text-sm text-slate-500">تحلیل قابل‌توضیح عملکرد بر اساس هدف فروش، تبدیل Lead، نظم پیگیری و سرعت پاسخ.</p></div>{error&&<div className="mb-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</div>}<div className="space-y-4">{rows.map(x=><div className="card p-5" key={x.id}><div className="flex flex-wrap items-center gap-3"><div className="grid h-12 w-12 place-items-center rounded-xl bg-brand-50 text-lg font-bold text-brand-700">{x.overall.toLocaleString('fa-IR')}</div><div><b>{x.name}</b><p className="mt-1 text-xs text-slate-500">امتیاز عملکرد ماه {x.month}</p></div><Badge tone={x.overall>=80?'green':x.overall>=65?'amber':x.overall>=45?'gray':'red'}>{x.label}</Badge></div><div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><div className="rounded-xl bg-slate-50 p-3"><small>تحقق هدف</small><b className="mt-1 block">{n(x.metrics.quotaPct)}٪</b></div><div className="rounded-xl bg-slate-50 p-3"><small>نرخ تبدیل</small><b className="mt-1 block">{n(x.metrics.conversion)}٪</b></div><div className="rounded-xl bg-slate-50 p-3"><small>نظم پیگیری</small><b className="mt-1 block">{n(x.metrics.discipline)}٪</b></div><div className="rounded-xl bg-slate-50 p-3"><small>سرعت پاسخ</small><b className="mt-1 block">{n(x.metrics.response)}٪</b></div></div><div className="mt-4 rounded-xl border border-brand-100 bg-brand-50/40 p-4"><b className="text-sm">پیشنهاد اصلی مربی</b><p className="mt-1 text-sm text-slate-700">{x.primaryAdvice}</p></div>{x.issues.length>0&&<div className="mt-4 grid gap-3 md:grid-cols-3">{x.issues.map(i=><div key={i.label} className="rounded-xl border p-3"><b className="text-sm">{i.label}</b><p className="mt-2 text-xs leading-5 text-slate-500">{i.advice}</p></div>)}</div>}</div>)}</div></>}
