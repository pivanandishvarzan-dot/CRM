'use client';import{useEffect,useState}from'react';import{Badge}from'./ui';const n=(x:number)=>Number(x||0).toLocaleString('fa-IR',{maximumFractionDigits:1});export default function SalesPlaybookDashboard(){const[d,setD]=useState<any>(null),[e,setE]=useState('');useEffect(()=>{fetch('/api/management/sales-playbook').then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error);setD(j.data)}).catch(x=>setE(x.message))},[]);if(e)return <div className="card p-6 text-red-700">{e}</div>;if(!d)return <div className="card p-6">در حال ساخت Playbook...</div>;return <><div className="mb-6"><p className="text-sm text-slate-500">Playbook پویا بر اساس Win/Loss واقعی دفتر</p><h1 className="mt-1 text-2xl font-bold">Adaptive Sales Playbook</h1><p className="mt-2 text-sm text-slate-500">به‌جای آموزش عمومی، روی علت‌هایی تمرکز می‌کند که واقعاً باعث Lost شدن مشتری‌های همین تیم شده‌اند.</p></div><div className="grid gap-3 sm:grid-cols-4">{[['ماژول فعال',d.summary.modules],['High Priority',d.summary.highPriority],['Win Rate',`${n(d.summary.winRate)}٪`],['Structured Loss',`${n(d.summary.structuredLossRate)}٪`]].map(([a,b]:any)=><div key={a} className="card p-4"><small>{a}</small><b className="block text-xl">{typeof b==='number'?n(b):b}</b></div>)}</div>{d.benchmark&&<div className="card mt-5 p-5"><small>Benchmark داخلی تیم</small><b className="mt-1 block text-lg">{d.benchmark.name} · Win Rate {n(d.benchmark.winRate)}٪</b><p className="mt-1 text-sm text-slate-500">فرایند این Agent می‌تواند در Coaching و بازطراحی Playbook بررسی شود.</p></div>}<div className="mt-5 grid gap-4 lg:grid-cols-2">{d.modules.map((x:any)=><section key={x.reason} className="card p-5"><div className="flex items-start justify-between gap-3"><div><Badge tone={x.priority==='HIGH'?'red':x.priority==='MEDIUM'?'amber':'green'}>{x.priority}</Badge><h2 className="mt-3 font-bold">{x.title}</h2><p className="mt-1 text-xs text-slate-500">{n(x.count)} Lost · {n(x.share)}٪ از باخت‌ها</p></div><div className="text-left"><small>Metric</small><b className="block text-sm">{x.metric}</b></div></div><ol className="mt-4 space-y-2">{x.actions.map((a:string,i:number)=><li key={i} className="rounded-xl border p-3 text-sm"><b className="ml-2">{(i+1).toLocaleString('fa-IR')}.</b>{a}</li>)}</ol></section>)}{!d.modules.length&&<div className="card p-8 text-center text-slate-500">برای ساخت Playbook هنوز Lost Reason کافی ثبت نشده است.</div>}</div></>}
