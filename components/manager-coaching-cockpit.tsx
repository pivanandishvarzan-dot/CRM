'use client';import{useEffect,useState}from'react';import{Badge}from'./ui';const n=(x:number)=>Number(x||0).toLocaleString('fa-IR',{maximumFractionDigits:1});export default function ManagerCoachingCockpit(){const[d,setD]=useState<any>(null),[e,setE]=useState('');useEffect(()=>{fetch('/api/management/manager-coaching').then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error);setD(j.data)}).catch(x=>setE(x.message))},[]);if(e)return <div className="card p-6 text-red-700">{e}</div>;if(!d)return <div className="card p-6">در حال ساخت Coaching Plan...</div>;return <><div className="mb-6"><p className="text-sm text-slate-500">{new Intl.DateTimeFormat('fa-IR-u-ca-persian',{weekday:'long',day:'numeric',month:'long',year:'numeric'}).format(new Date())}</p><h1 className="mt-1 text-2xl font-bold">Manager Coaching Cockpit</h1><p className="mt-2 text-sm text-slate-500">برنامه جلسه هفتگی مدیر با هر مشاور بر اساس Goal، Conversion، Follow-up و Pipeline.</p></div><div className="grid gap-3 sm:grid-cols-4">{[['مشاور فعال',d.summary.agents],['نیازمند Coaching',d.summary.needsCoaching],['Intervention',d.summary.intervention],['میانگین Coaching Score',d.summary.avgScore]].map(([a,b]:any)=><div key={a} className="card p-4"><small>{a}</small><b className="block text-2xl">{n(b)}</b></div>)}</div><div className="mt-5 space-y-4">{d.agents.map((a:any)=><section key={a.userId} className="card p-5"><div className="flex flex-wrap justify-between gap-4"><div><div className="flex gap-2"><Badge tone={a.status==='ON_TRACK'?'green':a.status==='COACHING'?'amber':'red'}>{a.status}</Badge><Badge tone={a.score>=80?'green':a.score>=60?'amber':'red'}>{n(a.score)} / ۱۰۰</Badge></div><h2 className="mt-3 text-xl font-bold">{a.name}</h2></div><div className="grid grid-cols-3 gap-4 text-center text-sm"><div><small>Goal</small><b className="block">{n(a.attainment)}٪</b></div><div><small>Conversion</small><b className="block">{n(a.conversion)}٪</b></div><div><small>Overdue</small><b className="block">{n(a.overdueFollowups)}</b></div></div></div><div className="mt-4 flex flex-wrap gap-2">{a.focus.map((x:string)=><span key={x} className="rounded-full border px-3 py-1 text-xs">{x}</span>)}</div><div className="mt-4 rounded-xl bg-slate-50 p-4 text-sm"><b>شروع پیشنهادی جلسه:</b> {a.meetingPrompt}</div><h3 className="mt-4 font-bold">تعهدهای این هفته</h3><div className="mt-2 grid gap-2 md:grid-cols-3">{a.actions.map((x:string,i:number)=><div key={i} className="rounded-xl border p-3 text-sm">{x}</div>)}</div></section>)}</div></>}
