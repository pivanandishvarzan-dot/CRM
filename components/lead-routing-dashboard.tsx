'use client';import{useEffect,useState}from'react';import Link from'next/link';import{Badge}from'./ui';const n=(x:number)=>Number(x||0).toLocaleString('fa-IR');export default function LeadRoutingDashboard(){const[d,setD]=useState<any>(null),[e,setE]=useState('');useEffect(()=>{fetch('/api/management/lead-routing').then(async r=>{const j=await r.json();if(!r.ok)throw new Error(j.error);setD(j.data)}).catch(x=>setE(x.message))},[]);if(e)return <div className="card p-6 text-red-700">{e}</div>;if(!d)return <div className="card p-6">در حال تحلیل تخصیص Lead...</div>;return <><div className="mb-6"><p className="text-sm text-slate-500">{new Intl.DateTimeFormat('fa-IR-u-ca-persian',{weekday:'long',day:'numeric',month:'long',year:'numeric'}).format(new Date())}</p><h1 className="mt-1 text-2xl font-bold">Smart Lead Routing</h1><p className="mt-2 text-sm text-slate-500">پیشنهاد Agent مناسب برای Leadهای بدون مسئول بر اساس ظرفیت، Conversion و وضعیت Coaching.</p></div><div className="grid gap-3 sm:grid-cols-3">{[['بدون مسئول',d.summary.unassigned],['قابل تخصیص',d.summary.routable],['Blocked',d.summary.blocked]].map(([a,b]:any)=><div key={a} className="card p-4"><small>{a}</small><b className="block text-2xl">{n(b)}</b></div>)}</div><div className="mt-5 space-y-4">{d.items.map((x:any)=><section key={x.id} className="card p-5"><div className="flex flex-wrap justify-between gap-4"><div><Link href={`/applicants/${x.id}`} className="font-bold underline">{x.name}</Link><p className="mt-1 text-xs text-slate-500">{x.status} · فوریت {n(x.urgency)}</p></div>{x.recommended?<div className="text-left"><small>پیشنهاد اول</small><b className="block">{x.recommended.name}</b><Badge tone={x.recommended.score>=70?'green':'amber'}>Fit {n(x.recommended.score)}</Badge></div>:<Badge tone="red">NO SAFE ROUTE</Badge>}</div><p className="mt-3 text-sm text-slate-600">{x.reason}</p>{x.alternatives.length>0&&<p className="mt-2 text-xs text-slate-400">جایگزین‌ها: {x.alternatives.map((a:any)=>`${a.name} (${n(a.score)})`).join('، ')}</p>}</section>)}{!d.items.length&&<div className="card p-8 text-center text-emerald-700">Lead بدون مسئول وجود ندارد.</div>}</div><p className="mt-4 text-xs text-slate-400">Routing در این مرحله فقط پیشنهاد است؛ تخصیص واقعی Lead بدون اقدام مدیر انجام نمی‌شود.</p></>}
