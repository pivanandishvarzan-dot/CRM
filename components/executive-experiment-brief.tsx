'use client';import Link from'next/link';import{useEffect,useState}from'react';import{Badge}from'./ui';export default function ExecutiveExperimentBrief(){const[d,setD]=useState<any>(null);useEffect(()=>{fetch('/api/management/lead-assignment/experiment/decision').then(r=>r.ok?r.json():null).then(j=>setD(j?.data||null)).catch(()=>{})},[]);if(!d?.experiment||!d.governance)return null;const g=d.governance,e=d.executiveSignal||{},risk=Number(d.risk?.score||0),readiness=Number(d.readiness?.score||0),open=(g.checks||[]).filter((x:any)=>!x.passed),priority=e.level==='CRITICAL'?'CRITICAL':e.level==='HIGH'?'HIGH':e.level==='MEDIUM'?'MEDIUM':open.length?'HIGH':'INFO',urgent=priority==='CRITICAL'||priority==='HIGH',tone=priority==='CRITICAL'||priority==='HIGH'?'red':priority==='MEDIUM'?'amber':'green',next=open[0],fallback=next?.code==='HEALTH'?'رفع هشدارهای Health آزمایش':next?.code==='RISK'?'مرور موانع Risk قبل از تغییر سهم':next?.code==='READINESS'?'تکمیل شواهد Readiness':next?.code==='AUDIT'?'بررسی پوشش Decision Audit':'مداخله‌ای لازم نیست',action=e.requiresExecutiveReview?'بررسی تصمیم توسط مدیر ارشد':fallback,why=e.reason||(priority==='HIGH'?`${open.length} کنترل مدیریتی باز است و قبل از تغییر سهم باید مرور شود.`:'کنترل‌های اصلی آزمایش در وضعیت پایدار هستند.'),trend=e.trend==='DETERIORATING'?'رو به وخامت':e.trend==='IMPROVING'?'رو به بهبود':e.trend==='WATCH'?'نیازمند پایش':'خط مبنا';return <section className={`card mb-5 p-5 ${urgent?'ring-2 ring-red-200':''}`}><div className="flex flex-wrap items-start justify-between gap-3"><div><div className="flex flex-wrap items-center gap-2"><h2 className="font-bold">سیگنال آزمایش امروز</h2><Badge tone={tone}>{priority==='CRITICAL'?'بحرانی':priority==='HIGH'?'مهم':priority==='MEDIUM'?'متوسط':'اطلاعاتی'}</Badge>{e.requiresExecutiveReview&&<Badge tone="red">نیازمند مدیر ارشد</Badge>}<span className="text-xs text-slate-400">اولویت مستقل برای Daily Brief</span></div><p className="mt-2 text-sm text-slate-600">تصمیم: <b>{d.decision}</b> · روند: <b>{trend}</b> · Governance {Number(g.score||0).toLocaleString('fa-IR')}/۱۰۰ · Readiness {readiness.toLocaleString('fa-IR')}/۱۰۰ · Risk {risk.toLocaleString('fa-IR')}/۱۰۰</p></div><Link href="/management/lead-assignment/experiment/decision" className="text-sm font-semibold text-sky-700">جزئیات تصمیم ←</Link></div><div className={`mt-4 rounded-xl p-4 text-sm ${urgent?'bg-red-50 text-red-800':priority==='MEDIUM'||open.length?'bg-amber-50 text-amber-800':'bg-emerald-50 text-emerald-800'}`}><div><b>اقدام پیشنهادی امروز:</b> {action}</div><p className="mt-1 text-xs opacity-80">{why}</p>{e.lastExecutedDecision&&<span className="mt-2 block text-xs opacity-80">آخرین تغییر سهم: {Number(e.lastExecutedDecision.from||0).toLocaleString('fa-IR')}٪ ← {Number(e.lastExecutedDecision.to||0).toLocaleString('fa-IR')}٪ · Confidence {Number(e.lastExecutedDecision.confidence||0).toLocaleString('fa-IR')}٪</span>}{open.length>1&&<span className="mt-1 block text-xs">{open.length.toLocaleString('fa-IR')} کنترل باز در مجموع</span>}</div></section>}
