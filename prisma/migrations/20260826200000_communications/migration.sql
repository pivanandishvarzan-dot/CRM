CREATE TABLE "Communication" ("id" TEXT NOT NULL,"agencyId" TEXT NOT NULL,"userId" TEXT NOT NULL,"applicantId" TEXT,"ownerId" TEXT,"channel" TEXT NOT NULL,"direction" TEXT NOT NULL DEFAULT 'OUTBOUND',"status" TEXT NOT NULL DEFAULT 'LOGGED',"subject" TEXT,"body" TEXT,"occurredAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,"nextActionAt" TIMESTAMP(3),"createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,CONSTRAINT "Communication_pkey" PRIMARY KEY ("id"));
CREATE INDEX "Communication_agencyId_occurredAt_idx" ON "Communication"("agencyId","occurredAt");CREATE INDEX "Communication_userId_occurredAt_idx" ON "Communication"("userId","occurredAt");CREATE INDEX "Communication_applicantId_occurredAt_idx" ON "Communication"("applicantId","occurredAt");CREATE INDEX "Communication_ownerId_occurredAt_idx" ON "Communication"("ownerId","occurredAt");
ALTER TABLE "Communication" ADD CONSTRAINT "Communication_agencyId_fkey" FOREIGN KEY ("agencyId") REFERENCES "Agency"("id") ON DELETE CASCADE ON UPDATE CASCADE;ALTER TABLE "Communication" ADD CONSTRAINT "Communication_userId_fkey" FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE;ALTER TABLE "Communication" ADD CONSTRAINT "Communication_applicantId_fkey" FOREIGN KEY ("applicantId") REFERENCES "Applicant"("id") ON DELETE CASCADE ON UPDATE CASCADE;ALTER TABLE "Communication" ADD CONSTRAINT "Communication_ownerId_fkey" FOREIGN KEY ("ownerId") REFERENCES "Owner"("id") ON DELETE CASCADE ON UPDATE CASCADE;
